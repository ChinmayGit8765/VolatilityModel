---
phase: 02-features-target-classical-baselines
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/volforecast/features/target.py
  - src/volforecast/eval/__init__.py
  - src/volforecast/eval/metrics.py
  - src/volforecast/eval/harness.py
  - tests/unit/test_target.py
  - tests/unit/test_metrics.py
  - tests/unit/test_harness.py
autonomous: false
requirements: [FEAT-01, EVAL-02, EVAL-03]
user_setup: []

must_haves:
  truths:
    - "qlike(x, x) == 0 holds for any positive variance array x"
    - "QLIKE, RMSE, MAE are importable from one shared module used by all later evaluation"
    - "The target is next-day variance of decimal log returns, defined once in target.py"
    - "A unit test FAILS if any walk-forward split is non-temporal or embargo < horizon"
    - "arch and statsmodels install into the uv environment"
  artifacts:
    - path: "src/volforecast/features/target.py"
      provides: "Canonical target definition (next-day squared decimal log-return variance) + HORIZON constant + unit docstring"
      contains: "def compute_target"
    - path: "src/volforecast/eval/metrics.py"
      provides: "Canonical qlike(), rmse(), mae() in Patton variance form"
      contains: "def qlike"
    - path: "src/volforecast/eval/harness.py"
      provides: "WalkForwardSplitter / walk_forward_splits generator with purge + embargo"
      contains: "def walk_forward_splits"
    - path: "tests/unit/test_harness.py"
      provides: "Leak test asserting temporal ordering and embargo >= horizon"
      contains: "max() < "
  key_links:
    - from: "src/volforecast/eval/metrics.py"
      to: "qlike(x,x)==0 unit test"
      via: "Patton variance form sigma2/h - ln(sigma2/h) - 1"
      pattern: "ratio - np.log\\(ratio\\) - 1"
    - from: "src/volforecast/eval/harness.py"
      to: "tests/unit/test_harness.py"
      via: "split invariant max(train_idx) < min(test_idx) and embargo >= horizon"
      pattern: "train_idx.max\\(\\) < .*test_idx.min\\(\\)"
---

<objective>
Build the numerical and correctness foundation that every later Phase 2 slice and every future phase consumes: install the two missing scientific packages, define the canonical realized-vol target once, define the canonical evaluation metrics once (QLIKE/RMSE/MAE), and build the purged walk-forward harness whose leak test is itself the project's credibility centrepiece.

Purpose: The bar ML must clear depends entirely on these three modules being correct. QLIKE has exactly one form in the codebase; the target has exactly one definition; the harness provably cannot leak. Every later plan imports these — no duplication, no skew.

Output: arch + statsmodels in pyproject/uv.lock; `target.py`, `eval/metrics.py`, `eval/harness.py`; three unit test files including the mandatory leak test and the mandatory `qlike(x,x)==0` test.
</objective>

## Phase Goal

**As a** quant reviewer of this volatility project, **I want to** see honest classical baselines (EWMA, GARCH(1,1), HAR-RV) scored on a canonically defined realized-vol target under a provably leak-free walk-forward harness, **so that** I can trust the published bar that Phase 3's ML model will be measured against.

(Note: the ROADMAP `**Goal:**` line for Phase 2 is in outcome-prose form, not the bolded user-story form. The story above is derived from that goal and the CONTEXT.md "eval-rigour centrepiece" framing; the genuine actor for this analytical phase is the reviewer/ML engineer.)

## Acceptance Criteria
- [ ] `uv run python -c "import arch, statsmodels"` succeeds inside the project venv
- [ ] `qlike(x, x)` returns 0 (within 1e-12) for any positive variance array
- [ ] The walk-forward leak test fails if the harness is mutated to emit a non-temporal or under-embargoed split
- [ ] All three new test files pass; `ruff check src tests` exits 0

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-target-classical-baselines/02-CONTEXT.md
@.planning/phases/02-features-target-classical-baselines/02-RESEARCH.md

<environment>
- Windows 11. `uv` is reachable only after `export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"` in bash. The project venv is `.venv/` (created by uv). Run all Python via `uv run` so the project venv (not system Python 3.13, which lacks pandas) is used.
- Tests are offline-only. `tests/conftest.py` sets `VOLFORECAST_NO_LIVE_API=1` at import. No task here touches the network.
- `arch` and `statsmodels` are NOT yet in pyproject.toml and NOT installed — Task 1 adds them.
- Python 3.12 is the pinned floor; the host also has 3.13. arch 8.0 supports 3.10–3.14, so either works.
</environment>

<interfaces>
<!-- Extracted from the existing codebase. Use these directly — no exploration needed. -->

From src/volforecast/config.py:
```python
def project_root() -> Path        # VOLFORECAST_ROOT env var, else cwd
def load_assets(config_path=None) -> list[dict]   # each dict: {symbol, asset_class, exchange}
def symbol_slug(symbol: str) -> str               # "BTC/USDT" -> "BTC-USD"
def processed_path(asset: dict, data_root=None) -> Path   # data/processed/{asset_class}/{slug}.parquet
```

From src/volforecast/ingest/base.py — the canonical OHLCV contract every processed parquet conforms to:
```python
OHLCV_COLUMNS: list[str] = ["open", "high", "low", "close", "volume"]
# Processed parquet: tz-aware UTC DatetimeIndex named "date" (normalized 00:00 UTC),
# float64 columns open/high/low/close/volume. Equity parquet has NO weekend rows
# (Phase 1 Pandera schema rejects them). Crypto is 24/7 (no gaps).
```
</interfaces>

<package_legitimacy>
Per 02-RESEARCH.md `## Package Legitimacy Audit`:
- `statsmodels` — [OK], ~15 yrs, statsmodels.org, no action.
- `arch` — [SUS] FALSE POSITIVE. slopcheck flagged it for phonetic proximity to "torch". This IS the canonical Kevin Sheppard financial-econometrics GARCH library (github.com/bashtage/arch, in production since ~2012, listed in the project STACK research). Manually verified on pypi.org/project/arch: author Kevin Sheppard, Production/Stable, requires Python>=3.10.
- Because `arch` is `[SUS]`, Task 0 below is a `checkpoint:human-verify gate="blocking-human"` legitimacy gate that runs BEFORE the install. It is never auto-approvable (see T-02-SC in the threat model).
</package_legitimacy>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate for arch (SUS — blocking)</name>
  <files>none — verification gate before install</files>
  <what-built>Nothing yet — this gate precedes the install. 02-RESEARCH flagged `arch` as [SUS] (slopcheck false positive vs "torch").</what-built>
  <action>Do NOT run the install yet. Pause and present the manual legitimacy verification below to the developer. This checkpoint is never auto-approvable (workflow.auto_advance is ignored for legitimacy gates per T-02-SC).</action>
  <how-to-verify>
    1. Open https://pypi.org/project/arch/ and confirm: author/maintainer is Kevin Sheppard, Development Status is Production/Stable, and the project description is "ARCH and other tools for financial econometrics" (GARCH/EWMA/volatility models).
    2. Confirm the source repository link points to github.com/bashtage/arch.
    3. Confirm `statsmodels` at https://pypi.org/project/statsmodels/ is the well-known statsmodels.org package.
    Expected: both are legitimate, long-established scientific packages; `arch` is the GARCH library this project's stack research selected, NOT a typosquat of torch.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms arch is the Kevin Sheppard GARCH library (github.com/bashtage/arch) and statsmodels is statsmodels.org — both legitimate.</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern.</resume-signal>
  <done>Developer has approved the arch + statsmodels install; the [SUS] false-positive is documented as resolved.</done>
</task>

<task type="auto">
  <name>Task 1: Add arch + statsmodels to the dependency matrix</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the existing `dependencies = [...]` array and pin style, e.g. "pandas>=2.3,<3")
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md (Standard Stack → "Missing from pyproject.toml")
  </read_first>
  <action>
    With PATH exported as documented in `<environment>`, run `uv add "arch>=8,<9" "statsmodels>=0.14"` from the project root. This updates `pyproject.toml` `dependencies` and regenerates `uv.lock` with hash-locked transitive deps. Do NOT hand-edit uv.lock. Pin form must match the existing matrix style (lower-bound + upper-bound where an upper bound matters). statsmodels is already pulled transitively by arch (>=0.13) but is pinned explicitly here to document the direct HAR-RV OLS dependency and avoid surprise upgrades (per D: Evaluation Report / research note). Do not add any other package.
  </action>
  <verify>
    <automated>uv run python -c "import arch, statsmodels; print(arch.__version__, statsmodels.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' pyproject.toml | grep -c 'arch>=8,<9'` returns 1
    - `grep -v '^#' pyproject.toml | grep -c 'statsmodels>=0.14'` returns 1
    - `uv run python -c "import arch, statsmodels"` exits 0 (no ModuleNotFoundError)
    - uv.lock is modified (git diff shows new arch/statsmodels entries)
  </acceptance_criteria>
  <done>arch (8.x) and statsmodels (0.14+) are installed in the project venv and pinned in pyproject.toml + uv.lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonical target + canonical metrics (QLIKE/RMSE/MAE)</name>
  <files>src/volforecast/features/target.py, src/volforecast/eval/__init__.py, src/volforecast/eval/metrics.py, tests/unit/test_target.py, tests/unit/test_metrics.py</files>
  <read_first>
    - src/volforecast/ingest/base.py (OHLCV_COLUMNS, the processed-parquet contract: UTC DatetimeIndex named "date", float64 close)
    - tests/unit/test_skeleton_e2e.py (test style: offline, fixture-driven, plain asserts)
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md (Pattern 4: QLIKE Patton variance form; "Multi-Lookback Realized Variance"; Pitfall 2: QLIKE arg order/units)
  </read_first>
  <behavior>
    target.py:
    - Test: compute_target on a close series produces next-day squared decimal log return; row t holds (ln(close[t+1]/close[t]))**2; the last row is NaN (no t+1 exists); units are decimal variance (~1e-4 for daily equity), NOT annualized, NOT percent.
    - Test: HORIZON == 1 (default next-day); horizon is a parameter so the harness/Phase 4 can reuse it.
    - Test: a 5-day forward realized variance helper (secondary stability check) sums the next 5 squared log returns / 5 and is NaN in the last 5 rows.
    metrics.py:
    - Test: qlike(x, x) == 0 within 1e-12 for x = array([0.001, 0.002, 0.0005]) — THE mandatory unit test.
    - Test: qlike(rv_var, forecast_var) is asymmetric — qlike(a, b) != qlike(b, a) for a != b.
    - Test: qlike never returns negative; forecast_var below QLIKE_FLOOR (1e-10) is clipped (no log(0)/division-by-zero, no NaN/Inf in output).
    - Test: rmse and mae match numpy reference on a small array.
  </behavior>
  <action>
    Create `src/volforecast/features/target.py` defining the canonical target once with a module docstring stating the unit convention: "daily VARIANCE of decimal log returns; vol is sqrt at report edges only; NO annualization inside the pipeline" (per CONTEXT Target & Units). Provide `HORIZON: int = 1` constant and `def compute_target(close: pd.Series, horizon: int = HORIZON) -> pd.Series` returning the next-`horizon` realized variance proxy. For horizon=1 the proxy is the next-day squared log return: `r_{t+1}**2` where `r = ln(close).diff()` and the value is shifted back so it is indexed at t (label window starts at t+1, no overlap with feature windows ending at t). Add `def forward_realized_var(close, window=5)` for the 5-day secondary stability check (mean of next `window` squared log returns). Both leave NaN where the forward window runs off the end — never fill with zero (Pitfall 4).

    Create `src/volforecast/eval/__init__.py` (empty package marker) and `src/volforecast/eval/metrics.py` defining the ONE canonical metrics set: `QLIKE_FLOOR = 1e-10`; `def qlike(rv_var, forecast_var) -> float` using the Patton (2011) variance form QLIKE = mean(ratio - ln(ratio) - 1) where ratio = rv_var / max(forecast_var, QLIKE_FLOOR). The signature order is `(realized, forecast)` — document that it is asymmetric and that arch returns conditional std dev which must be squared to variance before being passed here (Pitfall 2). Do NOT use the `log(h) + sigma2/h` form (it does not satisfy qlike(x,x)==0 — see research State of the Art). Add `def rmse(y_true, y_pred) -> float` and `def mae(y_true, y_pred) -> float`. All accept array-likes, coerce to float ndarray, and return scalar floats. No fenced code in this action — exact formula form is QLIKE = ratio - np.log(ratio) - 1 averaged.

    Write `tests/unit/test_target.py` and `tests/unit/test_metrics.py` covering every behavior above. Use plain pytest asserts, offline, no fixtures beyond small in-test arrays/series.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_target.py tests/unit/test_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_metrics.py` contains a test asserting `abs(qlike(x, x)) < 1e-12`
    - `grep -c "ratio - np.log(ratio) - 1" src/volforecast/eval/metrics.py` returns >= 1 (Patton form present)
    - `grep -c "QLIKE_FLOOR" src/volforecast/eval/metrics.py` returns >= 2 (defined + used in clip)
    - target.py module docstring contains the strings "variance", "decimal log returns", and "annualiz" (no-annualization statement)
    - `uv run pytest tests/unit/test_target.py tests/unit/test_metrics.py -q` exits 0
    - `uv run ruff check src/volforecast/features/target.py src/volforecast/eval/metrics.py tests/unit/test_target.py tests/unit/test_metrics.py` exits 0
  </acceptance_criteria>
  <done>One canonical target definition and one canonical metrics module exist, both unit-tested; `qlike(x,x)==0` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Purged walk-forward harness + mandatory leak test</name>
  <files>src/volforecast/eval/harness.py, tests/unit/test_harness.py</files>
  <read_first>
    - src/volforecast/features/target.py (HORIZON constant — the harness embargo must be >= this horizon)
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md (Pattern 6: walk-forward splitter, the exact min_train/step/horizon/purge/embargo semantics and the required leak test)
  </read_first>
  <behavior>
    - Test (THE leak test): for splits from walk_forward_splits(n=500, min_train=252, step=21, horizon=1), every split satisfies train_idx.max() < test_idx.min() AND test_idx.min() - train_idx.max() >= horizon. This test must FAIL if the generator is mutated to emit overlapping or insufficiently-embargoed splits.
    - Test (purge): with horizon=5, the last 5 training positions whose labels overlap the first test position are removed — train_idx.max() <= test_start - horizon - 1.
    - Test (expanding window): each successive split's train_idx is a strict superset prefix of the prior split's train range (expanding, not sliding); first split has at least min_train training observations.
    - Test (step): consecutive test windows advance by `step` positions; test windows are length `step` (except possibly the last, which may be shorter or is dropped if empty).
    - Test (no random split): split indices are contiguous integer ranges in ascending order — assert np.array_equal(train_idx, np.arange(0, train_idx.max()+1)) for the un-purged region.
  </behavior>
  <action>
    Create `src/volforecast/eval/harness.py` implementing the reusable, horizon-parameterized splitter per research Pattern 6. Provide a `@dataclass WalkForwardSplit` with `train_idx: np.ndarray` and `test_idx: np.ndarray` (integer positions into the full time series), and `def walk_forward_splits(n, min_train=252, step=21, horizon=1)` as a generator yielding WalkForwardSplit. Expanding window: training always starts at position 0. Purge: training end = test_start - horizon, so the last `horizon` train observations (whose labels overlap the first test obs) are dropped. Embargo: the gap between train end and test start is >= horizon by construction. Only yield a split when the purged training set still has >= min_train observations and the test window is non-empty. Document the enforced invariant in the docstring: max(train_idx) < min(test_idx) and min(test_idx) - max(train_idx) >= horizon. This is a LIBRARY function (Phase 4 promotion gate reuses it) — no script entry point, no I/O.

    Write `tests/unit/test_harness.py` covering every behavior above. The leak test is non-negotiable and is itself a portfolio talking point (CONTEXT specifics). Keep the assertions exact: `assert split.train_idx.max() < split.test_idx.min()` and `assert split.test_idx.min() - split.train_idx.max() >= horizon`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_harness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_harness.py` contains both `train_idx.max() < ` and `test_idx.min() - ` assertions (temporal ordering + embargo)
    - `grep -c "def walk_forward_splits" src/volforecast/eval/harness.py` returns 1
    - `grep -c "horizon" src/volforecast/eval/harness.py` returns >= 3 (purge + embargo + param all reference horizon)
    - Mutating train_end to `test_start` (removing the purge) makes the leak test FAIL — note this manually in the SUMMARY as evidence the test bites
    - `uv run pytest tests/unit/test_harness.py -q` exits 0
    - `uv run ruff check src/volforecast/eval/harness.py tests/unit/test_harness.py` exits 0
  </acceptance_criteria>
  <done>A reusable purged-expanding-window walk-forward harness exists with a leak test that fails on any non-temporal or under-embargoed split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → project env | `uv add` pulls arch/statsmodels + transitive deps from PyPI into the venv |
| processed parquet → in-memory frames | target/metrics consume Phase-1 parquet read via config.processed_path (own store, no untrusted external input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | QLIKE numerical inputs (NaN/Inf, log(0), div-by-zero) | mitigate | Clip forecast_var at QLIKE_FLOOR=1e-10 in metrics.py; unit test asserts qlike never returns negative/NaN |
| T-02-02 | Information disclosure | walk-forward harness leaking future data into train | mitigate | Purge + embargo>=horizon enforced in walk_forward_splits; leak test fails on any non-temporal split |
| T-02-03 | Tampering | parquet path traversal when reading processed data | accept | Paths come only from config.processed_path(project_root()+pathlib); no user-supplied path strings in this phase |
| T-02-SC | Tampering | `uv add arch statsmodels` supply-chain (arch flagged [SUS]) | mitigate | Task 0 blocking-human legitimacy checkpoint (never auto-approved) verifies arch=Kevin Sheppard GARCH lib via pypi.org/project/arch + github.com/bashtage/arch; uv.lock hash-locks resolved deps |
</threat_model>

<verification>
- `uv run python -c "import arch, statsmodels"` succeeds.
- `uv run pytest tests/unit/test_target.py tests/unit/test_metrics.py tests/unit/test_harness.py -q` all pass.
- `uv run ruff check src tests` exits 0.
- Manual: removing the harness purge makes the leak test fail (recorded in SUMMARY).
</verification>

<success_criteria>
- arch + statsmodels pinned and importable.
- One canonical target module, one canonical metrics module, one reusable walk-forward harness — each unit-tested.
- `qlike(x,x)==0` passes; leak test passes and provably bites.
</success_criteria>

<output>
Create `.planning/phases/02-features-target-classical-baselines/02-01-SUMMARY.md` when done.
</output>
