---
phase: 03-ml-challenger-serving
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/eval_lgbm.py
  - src/volforecast/eval/regimes.py
  - reports/ml_vs_baselines.md
  - reports/ml_vs_baselines.csv
  - tests/smoke/test_report.py
  - tests/unit/test_regimes.py
autonomous: true
requirements: [EVAL-04]
must_haves:
  truths:
    - "A committed ml_vs_baselines report compares LightGBM against EWMA, GARCH, and HAR-RV per asset using identical-fold QLIKE/RMSE/MAE"
    - "The report breaks results down per regime — per-asset vol terciles (low/mid/high) and calendar year"
    - "Where LightGBM loses to a baseline (e.g. HAR-RV), the report states it plainly rather than hiding it"
    - "Tercile cutoffs are computed on test-fold realized variance only (no lookahead)"
  artifacts:
    - path: "reports/ml_vs_baselines.md"
      provides: "honest ML-vs-baseline comparison per asset and per regime"
      contains: "QLIKE"
    - path: "reports/ml_vs_baselines.csv"
      provides: "machine-readable per-asset/per-regime metrics"
    - path: "src/volforecast/eval/regimes.py"
      provides: "vol-tercile + calendar-year regime labelling"
      exports: ["assign_vol_terciles", "assign_calendar_year"]
    - path: "scripts/eval_lgbm.py"
      provides: "report generator entry point"
  key_links:
    - from: "scripts/eval_lgbm.py"
      to: "reports/baseline_metrics.csv"
      via: "load baseline bar for comparison"
      pattern: "baseline_metrics"
    - from: "scripts/eval_lgbm.py"
      to: "models:/volforecast-lgbm@champion"
      via: "load champion for per-fold predictions"
      pattern: "volforecast-lgbm@champion"
---

<objective>
Generate `reports/ml_vs_baselines.md` (+ CSV): a per-asset and per-regime comparison of the LightGBM champion against the three Phase 2 baselines on identical walk-forward folds, with QLIKE/RMSE/MAE, stating plainly any regime/asset where ML loses.

Purpose: EVAL-04 and the project's eval-rigour centrepiece — "beating, or honestly not beating, GARCH". The Phase 2 bar (baseline_metrics.csv) already exists; this plan produces the apples-to-apples ML comparison and the honest per-regime narrative that the model card (Phase 5) and reviewers depend on.
Output: A regime-labelling library module, a report-generator script, the committed markdown + CSV report, and tests covering regime assignment and report completeness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ml-challenger-serving/03-RESEARCH.md
@.planning/phases/03-ml-challenger-serving/03-CONTEXT.md

<environment>
- Windows 11; uv NOT on PATH — prepend in fresh shells:
  `export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"`
- Compose stack UP. The report script loads the champion via `models:/volforecast-lgbm@champion` from `MLFLOW_TRACKING_URI=http://localhost:5000` (host side). Plan 02 must have registered the champion.
- reports/baseline_metrics.csv exists (Phase 2). reports/ are git-committed (not gitignored, unlike data/).
- The smoke test must run offline: build the report from a small in-memory fixture (mocked metrics / a synthetic comparison frame), never requiring MLflow or the compose stack.
</environment>

<interfaces>
From reports/baseline_metrics.csv (columns):
```
asset,model,n_forecasts,rmse,mae,qlike,garch_fallbacks
# model in {EWMA,GARCH,HAR}; 5 assets; HAR is best QLIKE on 4/5 assets
```

From src/volforecast/models/lgbm.py (Plans 01-02):
```python
KNOWN_ASSETS, ASSET_DTYPE, from_log_var, evaluate_per_asset, assemble_pooled_train
def evaluate_per_asset(model, asset_feature_dfs, asset_target_series, ...) -> dict
    # {symbol: {"rmse","mae","qlike","n_forecasts"}} on the variance scale, identical folds
```

From src/volforecast/eval/{harness,metrics}.py and features/target.py:
```python
def walk_forward_splits(n, min_train=252, step=21, horizon=1)
def qlike(rv_var, forecast_var) -> float ; rmse ; mae
def compute_target(close, horizon=1) -> pd.Series   # realized variance proxy at t
```

MLflow champion load (RESEARCH Pattern 5):
```python
import mlflow.pyfunc
model = mlflow.pyfunc.load_model("models:/volforecast-lgbm@champion")  # .predict(df) -> log-var; apply from_log_var
```

Regime definition (CONTEXT + RESEARCH Open Question #3):
- vol terciles: low/mid/high computed on TEST-FOLD realized variance only, per asset (no lookahead)
- calendar year: from the as-of date index of each test row
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regime labelling library (vol terciles + calendar year)</name>
  <files>src/volforecast/eval/regimes.py, tests/unit/test_regimes.py</files>
  <read_first>
    - src/volforecast/eval/metrics.py (module style/docstring conventions to mirror)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Open Question #3 — terciles on test-fold realized variance only)
    - .planning/phases/03-ml-challenger-serving/03-CONTEXT.md (Regimes: per-asset vol terciles low/mid/high + calendar year)
  </read_first>
  <behavior>
    - assign_vol_terciles(realized_var: pd.Series) labels each row low/mid/high by the 33rd/67th percentiles of THAT series (test-fold realized variance), per asset
    - boundaries are inclusive/consistent: roughly one-third of rows in each bucket for a uniform input
    - assign_calendar_year(index: DatetimeIndex) returns the year (int) per row from the as-of date
    - terciles computed only on the provided (test-fold) series — function has no access to train data, structurally preventing lookahead
    - constant-variance edge case (all equal) does not raise; degenerate buckets are handled gracefully
  </behavior>
  <action>
    Create `src/volforecast/eval/regimes.py`. Implement `assign_vol_terciles(realized_var: pd.Series) -> pd.Series` returning a categorical Series of {"low","mid","high"} using the 1/3 and 2/3 quantiles of the input series (the test-fold realized variance per asset; Open Question #3 — no train data, no lookahead). Implement `assign_calendar_year(index: pd.DatetimeIndex) -> pd.Series` returning the integer year per row. Document that terciles are deliberately computed on test-fold realizations only. Handle the constant/degenerate input without raising.
    Create `tests/unit/test_regimes.py` covering the <behavior> cases (offline, synthetic series).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run pytest tests/unit/test_regimes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_regimes.py -x -q` passes including the ~1/3-per-bucket and constant-input cases.
    - `uv run python -c "from volforecast.eval.regimes import assign_vol_terciles, assign_calendar_year; print('ok')"` prints ok.
    - `uv run ruff check src/volforecast/eval/regimes.py tests/unit/test_regimes.py` clean.
  </acceptance_criteria>
  <done>Regime labels (vol terciles on test-fold realized variance + calendar year) are produced by a tested library function with no lookahead.</done>
</task>

<task type="auto">
  <name>Task 2: Report generator script — per-asset + per-regime ML-vs-baseline comparison</name>
  <files>scripts/eval_lgbm.py, reports/ml_vs_baselines.md, reports/ml_vs_baselines.csv</files>
  <read_first>
    - reports/baseline_eval.md and reports/baseline_metrics.csv (the Phase 2 report style + columns to mirror and compare against)
    - scripts/generate_features.py (load-assets + parquet-load + sys.path pattern)
    - scripts/train_lgbm.py (how the champion is loaded / how per-asset eval is run — reuse, do not re-implement scoring)
    - .planning/phases/03-ml-challenger-serving/03-CONTEXT.md (SHAP & Report: losses stated plainly per asset/regime)
  </read_first>
  <action>
    Create `scripts/eval_lgbm.py` (run: `uv run python scripts/eval_lgbm.py`). Load the champion via `mlflow.pyfunc.load_model("models:/volforecast-lgbm@champion")` (tracking URI from env, default localhost:5000). For each asset, walk the SAME `walk_forward_splits` test folds, collect per-test-row: as-of date, realized variance (compute_target), and LightGBM forecast variance (`from_log_var(model.predict(test_X))`). Label each test row with `assign_vol_terciles` (per-asset, test-fold realized var) and `assign_calendar_year`.
    Compute LightGBM QLIKE/RMSE/MAE per asset overall and within each regime cell (asset x tercile, asset x year) using eval/metrics.py. Load reports/baseline_metrics.csv for the per-asset EWMA/GARCH/HAR bar; for the per-regime breakdown, recompute the three baselines' per-regime metrics on the same test rows OR — to keep scope bounded — recompute the baseline forecasts per fold using the existing `models/{ewma,garch,har_rv}.py` aligned forecasts on the same test indices (Claude's discretion on whichever reuses existing machinery without duplicating metric logic).
    Write `reports/ml_vs_baselines.csv` (columns: asset, regime_type, regime_value, model, n, rmse, mae, qlike) and `reports/ml_vs_baselines.md` with: (a) per-asset overall table LightGBM vs EWMA/GARCH/HAR, (b) per-tercile and per-year breakdown tables, (c) an explicit honest-findings section that names every asset/regime where LightGBM's QLIKE is worse than the best baseline (do NOT soften or omit losses — CONTEXT specific idea). Mirror the heading/table style of reports/baseline_eval.md.
    Commit both report files (reports/ is tracked, not gitignored).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run python scripts/eval_lgbm.py && uv run python -c "import pandas as pd,pathlib; df=pd.read_csv('reports/ml_vs_baselines.csv'); assert set(df['asset'].unique()) >= {'BTC-USD','ETH-USD','SPY','AAPL','MSFT'}; assert {'LightGBM','EWMA','GARCH','HAR'} <= set(df['model'].unique()); assert pathlib.Path('reports/ml_vs_baselines.md').read_text(encoding='utf-8').find('QLIKE')>=0; print('report ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/eval_lgbm.py` runs and writes reports/ml_vs_baselines.md and reports/ml_vs_baselines.csv.
    - CSV contains all 5 assets and the 4 models (LightGBM + EWMA/GARCH/HAR), with per-regime rows for both regime_type values (tercile, year).
    - The markdown contains an explicit section naming any asset/regime where LightGBM loses to the best baseline (HAR-RV is best QLIKE on 4/5 assets per the Phase 2 bar — expect honest losses).
    - LightGBM is scored via eval/metrics.py on the variance scale on the identical folds (reuses harness + metrics, no new metric implementation).
    - `uv run ruff check scripts/eval_lgbm.py` clean.
  </acceptance_criteria>
  <done>The committed report honestly compares LightGBM to all three baselines per asset and per regime on identical folds, stating losses plainly.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test for report completeness</name>
  <files>tests/smoke/test_report.py</files>
  <read_first>
    - scripts/eval_lgbm.py (the report-writing functions to import; refactor a pure `render_report(comparison_df) -> str` if needed so it is testable without MLflow)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Validation Architecture — smoke test note; integration tests must not start the compose stack)
  </read_first>
  <action>
    Ensure the markdown rendering in scripts/eval_lgbm.py is factored into a pure function (e.g. `render_report(per_asset_df, per_regime_df) -> str`) that takes already-computed metric frames and returns the markdown string — so it is testable offline without MLflow or the compose stack. Create `tests/smoke/test_report.py` that feeds a small synthetic comparison frame (all 5 assets, 4 models, at least one regime where the synthetic LightGBM QLIKE is worse than a baseline) into `render_report` and asserts: all 5 asset names appear, all 4 model names appear, the tercile labels (low/mid/high) and at least one calendar year appear, and the honest-losses section names the constructed losing asset/regime. Create the `tests/smoke/` directory if absent. No MLflow, no compose stack, no network.
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run pytest tests/smoke/test_report.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/smoke/test_report.py -x -q` passes, runs fully offline (no MLflow/compose).
    - The test asserts presence of all 5 assets, 4 models, regime labels, and a named honest loss in the rendered markdown.
    - `uv run pytest tests/ -x -q` (full suite) green.
    - `uv run ruff check tests/smoke/test_report.py` clean.
  </acceptance_criteria>
  <done>Report completeness (all assets, all baselines, per-regime breakdown, honest losses) is asserted by an offline smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLflow registry → report script (champion load) | Pickled model loaded via alias on loopback |
| report output → git repo | Report committed publicly; must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | Pickle deserialization via pyfunc.load_model | mitigate | Load only from this project's own registry by alias; provenance verified by Plan 02 lineage tags |
| T-03-07 | Information Disclosure | committed report files | mitigate | Report contains only metrics + dates (no secrets, no credentials); reviewed before commit |
| T-03-08 | Tampering | metric integrity (apples-to-apples) | mitigate | LightGBM scored with the same eval/metrics.py + identical folds as baselines — no parallel metric implementation that could diverge |
</threat_model>

<verification>
- reports/ml_vs_baselines.md + .csv generated and committed; all 5 assets, 4 models, per-regime breakdown present.
- Honest losses section names every asset/regime where ML underperforms the best baseline.
- `uv run pytest tests/ -x -q` green; `uv run ruff check .` clean.
</verification>

<success_criteria>
- EVAL-04: ML-vs-baseline comparison per asset AND per regime, reported honestly even where ML loses, on identical folds with canonical metrics.
</success_criteria>

<output>
Create `.planning/phases/03-ml-challenger-serving/03-03-SUMMARY.md` when done.
</output>
