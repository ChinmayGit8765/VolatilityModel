---
phase: 02-features-target-classical-baselines
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/volforecast/features/estimators.py
  - src/volforecast/models/__init__.py
  - src/volforecast/models/ewma.py
  - src/volforecast/reports/__init__.py
  - src/volforecast/reports/baseline.py
  - reports/baseline_eval.md
  - reports/baseline_metrics.csv
  - tests/unit/test_estimators.py
  - tests/unit/test_ewma.py
  - tests/unit/test_report_e2e.py
autonomous: true
requirements: [FEAT-02, EVAL-01, EVAL-03]
user_setup: []

must_haves:
  truths:
    - "A reviewer can open reports/baseline_eval.md and see per-asset EWMA RMSE/MAE/QLIKE for all 5 assets"
    - "EWMA produces walk-forward forecasts per asset using the harness from 02-01"
    - "EWMA variance uses RiskMetrics lambda=0.94 documented once"
    - "The report and CSV are reproducible from a single command"
  artifacts:
    - path: "src/volforecast/models/ewma.py"
      provides: "EWMA(lambda=0.94) variance forecaster with fit/predict interface"
      contains: "EWMA_LAMBDA"
    - path: "src/volforecast/reports/baseline.py"
      provides: "Report generator: runs harness, scores baselines with metrics, writes md + csv"
      contains: "def generate_baseline_report"
    - path: "reports/baseline_eval.md"
      provides: "The published bar — per-asset baseline metrics table"
      contains: "QLIKE"
    - path: "reports/baseline_metrics.csv"
      provides: "Machine-readable per-asset metrics"
      contains: "qlike"
  key_links:
    - from: "src/volforecast/reports/baseline.py"
      to: "src/volforecast/eval/harness.py + eval/metrics.py + features/target.py"
      via: "imports walk_forward_splits, qlike/rmse/mae, compute_target"
      pattern: "from volforecast.eval|from volforecast.features.target"
    - from: "src/volforecast/models/ewma.py"
      to: "reports/baseline.py"
      via: "baseline runner calls EWMA forecast per walk-forward fold"
      pattern: "ewma|EWMA"
---

<objective>
Deliver the first real, user-visible artifact of the phase: a committed `reports/baseline_eval.md` (plus machine-readable CSV) showing per-asset EWMA RMSE/MAE/QLIKE across all 5 assets, produced end-to-end through the canonical target, the leak-free harness, the EWMA baseline, and the canonical metrics. This is the thinnest complete vertical slice — target → minimal features → harness → ONE baseline → metrics → published report.

Purpose: After this plan a reviewer can open the published bar and read honest per-asset numbers. GARCH and HAR-RV (Plan 03) and the full ML feature matrix (Plan 04) then extend this exact report rather than inventing a new one. EWMA is chosen first because it is fully deterministic (no optimizer, no convergence risk), so the end-to-end pipeline is proven before GARCH's numerical hazards are introduced.

Output: `features/estimators.py` (RV + log/squared returns + EWMA variance helpers), `models/ewma.py`, `reports/baseline.py`, the committed `reports/baseline_eval.md` and `reports/baseline_metrics.csv`, and unit + end-to-end tests.
</objective>

## Phase Goal

**As a** quant reviewer of this volatility project, **I want to** see honest classical baselines scored on a canonical realized-vol target under a leak-free harness, **so that** I can trust the published bar that Phase 3's ML will be measured against.

## Acceptance Criteria
- [ ] `reports/baseline_eval.md` exists and lists all 5 assets (BTC-USD, ETH-USD, SPY, AAPL, MSFT) with EWMA RMSE/MAE/QLIKE
- [ ] `reports/baseline_metrics.csv` has one row per (asset, model) with columns asset, model, rmse, mae, qlike, n_forecasts
- [ ] EWMA forecasts are produced per walk-forward fold from the 02-01 harness (no random splits)
- [ ] EWMA variance uses lambda=0.94 with `ewm(adjust=False)`
- [ ] End-to-end test reproduces a report from a small fixture; `ruff check src tests` exits 0

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-features-target-classical-baselines/02-CONTEXT.md
@.planning/phases/02-features-target-classical-baselines/02-RESEARCH.md
@.planning/phases/02-features-target-classical-baselines/02-01-SUMMARY.md

<environment>
- `uv` reachable only after `export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"`. Run Python via `uv run`.
- Trusted inputs: `data/processed/crypto/{BTC-USD,ETH-USD}.parquet` and `data/processed/equity/{SPY,AAPL,MSFT}.parquet`. Each has tz-aware UTC DatetimeIndex named "date", float64 OHLCV columns, no weekend rows for equities.
- Offline-only tests. The end-to-end report test must run on a SMALL committed fixture (not the full processed data) so CI stays fast and offline.
- The 5-asset universe is read via `config.load_assets()` / `config.processed_path(asset)`.
</environment>

<interfaces>
<!-- From 02-01 (this plan's dependency). Import these directly. -->

From src/volforecast/features/target.py (built in 02-01):
```python
HORIZON: int = 1
def compute_target(close: pd.Series, horizon: int = HORIZON) -> pd.Series   # next-day decimal variance proxy; last row NaN
```

From src/volforecast/eval/metrics.py (built in 02-01):
```python
QLIKE_FLOOR = 1e-10
def qlike(rv_var, forecast_var) -> float   # Patton variance form; asymmetric; arg order (realized, forecast)
def rmse(y_true, y_pred) -> float
def mae(y_true, y_pred) -> float
```

From src/volforecast/eval/harness.py (built in 02-01):
```python
@dataclass
class WalkForwardSplit: train_idx: np.ndarray; test_idx: np.ndarray
def walk_forward_splits(n, min_train=252, step=21, horizon=1) -> Generator[WalkForwardSplit]
```

From src/volforecast/config.py (Phase 1):
```python
def load_assets(config_path=None) -> list[dict]   # {symbol, asset_class, exchange}
def processed_path(asset, data_root=None) -> Path
def symbol_slug(symbol) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing end-to-end report test (RED) + return-and-RV estimators</name>
  <files>tests/unit/test_report_e2e.py, src/volforecast/features/estimators.py, tests/unit/test_estimators.py</files>
  <read_first>
    - src/volforecast/features/target.py (compute_target; the variance-units convention)
    - src/volforecast/eval/harness.py and src/volforecast/eval/metrics.py (the 02-01 interfaces above)
    - tests/conftest.py (offline guard, fixture style) and tests/fixtures/ (existing parquet fixtures: crypto_sample.parquet, equity_sample.parquet)
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md ("Multi-Lookback Realized Variance" and Pattern 3 EWMA variance; Pitfall 3 integer-window rolling)
  </read_first>
  <behavior>
    estimators.py:
    - Test: `log_returns(close)` == ln(close).diff(); first row NaN.
    - Test: `squared_returns(close)` == log_returns(close)**2.
    - Test: `realized_var(log_returns, window=5)` == log_returns.pow(2).rolling(5).mean() using an INTEGER window (count-based, not "5D" duration — Pitfall 3); first 4 rows NaN.
    - Test: `ewma_variance(log_returns, lam=0.94)` == log_returns.pow(2).ewm(alpha=1-0.94, adjust=False).mean(); EWMA_LAMBDA constant == 0.94; adjust=False is mandatory (Pattern 3 warning).
    test_report_e2e.py (RED at this task — generate_baseline_report does not exist yet):
    - Test: generate_baseline_report(assets_subset, output_dir) writes baseline_eval.md and baseline_metrics.csv to output_dir; the md contains each asset slug and the columns RMSE/MAE/QLIKE; the csv has one row per (asset, model="EWMA") with numeric metrics and n_forecasts > 0. This test imports from volforecast.reports.baseline and MUST fail now with ImportError/AttributeError (RED). Use a tiny 2-asset fixture (crypto_sample + equity_sample) with min_train small enough (e.g. 30) that the harness yields >=1 fold on the fixture length.
  </behavior>
  <action>
    Create `src/volforecast/features/estimators.py` with the deterministic estimator helpers used both as features (Plan 04) and as the EWMA baseline input: `def log_returns(close: pd.Series) -> pd.Series` (natural log diff), `def squared_returns(close) -> pd.Series`, `EWMA_LAMBDA = 0.94` module constant, `def ewma_variance(log_returns_decimal: pd.Series, lam: float = EWMA_LAMBDA) -> pd.Series` using `.pow(2).ewm(alpha=1-lam, adjust=False).mean()` (adjust=False is non-negotiable — adjust=True diverges on early values), and `def realized_var(log_returns: pd.Series, window: int) -> pd.Series` as `.pow(2).rolling(window).mean()` with an INTEGER window. Document units in the module docstring: decimal log-return variance, no annualization. All windows end strictly at as-of t (rolling is right-aligned by default — confirm with a test).

    Write `tests/unit/test_estimators.py` covering every estimator behavior above against small in-test series and the existing crypto/equity fixtures.

    Write `tests/unit/test_report_e2e.py` as the RED end-to-end test described in `<behavior>`. It targets the not-yet-existing `volforecast.reports.baseline.generate_baseline_report`. Commit this test in a failing state with a `test(02-02): ...RED` commit message so the RED→GREEN gate is visible.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_estimators.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adjust=False" src/volforecast/features/estimators.py` returns >= 1
    - `grep -c "EWMA_LAMBDA = 0.94" src/volforecast/features/estimators.py` returns 1
    - `uv run pytest tests/unit/test_estimators.py -q` exits 0
    - `uv run pytest tests/unit/test_report_e2e.py -q` FAILS at this task (RED — ImportError/AttributeError on generate_baseline_report)
    - `uv run ruff check src/volforecast/features/estimators.py tests/unit/test_estimators.py tests/unit/test_report_e2e.py` exits 0
  </acceptance_criteria>
  <done>Estimator helpers exist and pass; the end-to-end report test exists and fails (RED), defining the contract Task 3 implements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EWMA baseline forecaster (walk-forward compatible)</name>
  <files>src/volforecast/models/__init__.py, src/volforecast/models/ewma.py, tests/unit/test_ewma.py</files>
  <read_first>
    - src/volforecast/features/estimators.py (ewma_variance, EWMA_LAMBDA — reuse, do not re-derive)
    - src/volforecast/eval/harness.py (WalkForwardSplit shape — the forecaster consumes train/test index pairs)
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md (Pattern 3 EWMA recursion)
  </read_first>
  <behavior>
    - Test: EWMA forecast for test position i is the EWMA variance computed from all log returns strictly up to and including i-1 (one-step-ahead, no look-ahead). Equivalent to ewma_variance(returns).shift(1) evaluated at test indices.
    - Test: lambda defaults to 0.94 and is configurable.
    - Test: forecasts are positive and finite for a non-degenerate return series.
    - Test: running EWMA across a list of walk_forward_splits over a 300-point series yields one forecast per test index, aligned to the test index.
  </behavior>
  <action>
    Create `src/volforecast/models/ewma.py` with an `EWMA` forecaster exposing a minimal, harness-friendly interface consistent with how Plan 03's GARCH/HAR will be called: a class `EWMA(lam: float = EWMA_LAMBDA)` with `forecast_path(log_returns: pd.Series) -> pd.Series` returning the one-step-ahead variance forecast aligned to each date (the EWMA variance using only returns < t — i.e. `ewma_variance(log_returns, lam).shift(1)`). EWMA is deterministic and needs no per-fold refit, but expose the same call shape (given returns up to as-of, return next-step variance) so the baseline runner treats all three baselines uniformly. Reuse `ewma_variance` and `EWMA_LAMBDA` from estimators.py — do not duplicate the recursion. Make `src/volforecast/models/__init__.py` export `EWMA`.

    Write `tests/unit/test_ewma.py` covering the no-look-ahead and alignment behaviors above.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ewma.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shift(1)" src/volforecast/models/ewma.py` returns >= 1 (one-step-ahead, no look-ahead)
    - `grep -c "from volforecast.features.estimators import" src/volforecast/models/ewma.py` returns >= 1 (reuse, not re-derive)
    - test_ewma.py asserts the forecast at index t uses only returns strictly before t
    - `uv run pytest tests/unit/test_ewma.py -q` exits 0
    - `uv run ruff check src/volforecast/models/ewma.py tests/unit/test_ewma.py` exits 0
  </acceptance_criteria>
  <done>A deterministic, no-look-ahead EWMA forecaster exists with a uniform baseline call shape; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Baseline report generator → reports/baseline_eval.md (GREEN)</name>
  <files>src/volforecast/reports/__init__.py, src/volforecast/reports/baseline.py, reports/baseline_eval.md, reports/baseline_metrics.csv</files>
  <read_first>
    - tests/unit/test_report_e2e.py (the RED contract from Task 1 — this task makes it pass)
    - src/volforecast/eval/harness.py, eval/metrics.py, features/target.py, models/ewma.py (all the pieces being wired)
    - src/volforecast/config.py (load_assets, processed_path — the 5-asset universe)
    - .planning/phases/02-features-target-classical-baselines/02-RESEARCH.md (Open Questions #3: per-asset layout; report = Reporting tier in Responsibility Map)
  </read_first>
  <behavior>
    - Test (the RED e2e test now GREEN): generate_baseline_report on a 2-asset fixture writes baseline_eval.md (per-asset RMSE/MAE/QLIKE table) and baseline_metrics.csv (rows per asset/model) with n_forecasts > 0.
    - Test: metrics are computed by comparing EWMA forecast variance against compute_target realized variance ONLY on walk-forward test indices (not the whole series), so no in-sample leakage.
    - Test: a forecast/target row with NaN target (end-of-series) is dropped before scoring (Pitfall 4 — never fill with zero).
    - Test: QLIKE is fed variance vs variance (not std dev) — values are non-negative.
  </behavior>
  <action>
    Create `src/volforecast/reports/__init__.py` and `src/volforecast/reports/baseline.py` with `def generate_baseline_report(assets: list[dict], output_dir: Path, min_train: int = 252, step: int = 21) -> Path`. For each asset: read its processed parquet via `config.processed_path`, compute `log_returns(close)`, `compute_target(close)` (realized variance label), generate EWMA one-step-ahead forecasts, then iterate `walk_forward_splits(len(series), min_train, step, horizon=HORIZON)` collecting forecast/target pairs at every test index across all folds. Drop pairs where the target is NaN. Score the collected pairs with `qlike(realized, forecast)`, `rmse`, `mae` from the canonical metrics module. Write a Markdown table (one row per asset: asset, model, n_forecasts, RMSE, MAE, QLIKE) to `{output_dir}/baseline_eval.md` with a header noting units (daily decimal variance; vol shown as sqrt only at the edges; no annualization) and that EWMA is the first baseline with GARCH/HAR-RV added in 02-03. Write the same data as `{output_dir}/baseline_metrics.csv` (columns: asset, model, n_forecasts, rmse, mae, qlike). The report must be HONEST and per-asset (CONTEXT specifics — "even where baselines are strong"). This module is a library function callable from tests with a small fixture dir AND from a CLI/script with the real 5-asset universe.

    After implementation passes the e2e test, run it against the REAL 5-asset processed data (min_train=252, step=21) and commit the resulting `reports/baseline_eval.md` and `reports/baseline_metrics.csv` — these committed artifacts are "the published bar" (CONTEXT Evaluation Report).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_report_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def generate_baseline_report" src/volforecast/reports/baseline.py` returns 1
    - `grep -E "from volforecast.eval|from volforecast.features.target" src/volforecast/reports/baseline.py` matches (canonical modules imported, not re-implemented)
    - `reports/baseline_eval.md` exists and `grep -c "QLIKE" reports/baseline_eval.md` returns >= 1
    - `reports/baseline_eval.md` mentions all 5 slugs: BTC-USD, ETH-USD, SPY, AAPL, MSFT
    - `reports/baseline_metrics.csv` header contains `asset,model` and a `qlike` column; has >= 5 data rows
    - `uv run pytest tests/unit/test_report_e2e.py -q` exits 0 (RED→GREEN complete)
    - `uv run ruff check src/volforecast/reports/baseline.py` exits 0
  </acceptance_criteria>
  <done>A reproducible report generator wires target+harness+EWMA+metrics; the committed reports/baseline_eval.md + CSV show honest per-asset EWMA metrics for all 5 assets — the first published bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processed parquet → report pipeline | report reads Phase-1 processed parquet (own store) via config paths |
| in-memory metrics → committed report files | report writes md/csv to reports/ under project_root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | in-sample leakage when scoring EWMA | mitigate | Metrics computed only on walk_forward_splits test indices (02-01 harness); e2e test asserts forecasts use only data < t |
| T-02-05 | Tampering | NaN target rows (end-of-series) silently scored as zero | mitigate | Drop NaN target pairs before scoring; never fill with zero (Pitfall 4); test covers this |
| T-02-06 | Tampering | report output path traversal | accept | output_dir derived from project_root()/reports via pathlib; no user-supplied path strings |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_estimators.py tests/unit/test_ewma.py tests/unit/test_report_e2e.py -q` all pass.
- `reports/baseline_eval.md` and `reports/baseline_metrics.csv` exist, committed, covering all 5 assets.
- `uv run ruff check src tests` exits 0.
</verification>

<success_criteria>
- First end-to-end vertical slice complete: a reviewer can open the published bar and read honest per-asset EWMA RMSE/MAE/QLIKE.
- All scoring runs only on leak-free walk-forward test folds via the 02-01 harness.
- The report generator is a reusable library that Plan 03 extends with GARCH and HAR-RV.
</success_criteria>

<output>
Create `.planning/phases/02-features-target-classical-baselines/02-02-SUMMARY.md` when done.
</output>
