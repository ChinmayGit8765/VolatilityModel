---
phase: 05-dashboard-honest-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MODEL_CARD.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "A reviewer can read honest, regime-segmented metrics for LightGBM vs EWMA / GARCH(1,1) / HAR-RV per asset"
    - "The card explicitly names every asset and regime where LightGBM LOSES to a classical baseline (especially the high-vol QLIKE blowups)"
    - "The card states intended use (a forecasting + MLOps DEMO, explicitly NOT a trading strategy), training window, leak-free purged walk-forward methodology, the QLIKE metric definition, assumptions, and known limitations"
    - "Every number in the card is traceable to reports/ml_vs_baselines.md / .csv / reports/baseline_eval.md — no invented metrics"
  artifacts:
    - path: "MODEL_CARD.md"
      provides: "Honest model card for volforecast-lgbm with per-asset + per-regime tables and a losses section"
      min_lines: 90
      contains: "QLIKE"
  key_links:
    - from: "MODEL_CARD.md"
      to: "reports/ml_vs_baselines.md"
      via: "per-asset and per-regime numbers copied verbatim from the source report"
      pattern: "LightGBM"
    - from: "MODEL_CARD.md"
      to: "reports/ml_vs_baselines.csv"
      via: "machine-readable provenance for the same numbers"
      pattern: "ml_vs_baselines"
---

<objective>
Author MODEL_CARD.md: an honest, regime-segmented account of how the registered
`volforecast-lgbm` champion actually performed against the three classical baselines
(EWMA, GARCH(1,1), HAR-RV) under leak-free purged walk-forward evaluation.

Purpose: DOCS-01 — the credibility centerpiece. The project's value is honest
benchmarking; the card must report the real finding (LightGBM wins on RMSE/MAE in many
asset-overall views but loses badly on QLIKE, catastrophically in high-vol regimes) and
name every loss plainly, with all numbers sourced from the committed report files.

Output: a single MODEL_CARD.md at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-honest-documentation/05-CONTEXT.md
@CLAUDE.md

<source_of_truth>
ALL metrics MUST be pulled verbatim from these committed files — do NOT invent or
recompute any number:
  - reports/ml_vs_baselines.md  — per-asset overall, per-vol-tercile, per-calendar-year
    tables for LightGBM vs EWMA/GARCH/HAR, plus a "Section 4: Honest Findings — Where
    LightGBM Underperforms" list that already enumerates every loss with deltas.
  - reports/ml_vs_baselines.csv — machine-readable source of the same numbers.
  - reports/baseline_eval.md     — baseline-only EWMA/GARCH/HAR per-asset metrics and
    the unit/protocol definitions (QLIKE Patton 2011 variance form; min_train=252,
    step=21 walk-forward; EWMA lambda=0.94; GARCH on 100x-scaled returns; HAR Corsi 2009).

Headline finding from reports/ml_vs_baselines.md (use as the card's honest thesis):
  - LightGBM beats the classical baselines on RMSE and MAE in the per-asset-overall and
    low/mid-vol tercile views, but its QLIKE is WORSE (higher) than the best classical
    baseline for every asset overall and, most starkly, in the high-vol tercile
    (e.g. BTC-USD high-vol: LightGBM QLIKE 11.50 vs GARCH 0.68; ETH-USD high-vol:
    LightGBM 13.88 vs HAR 1.10). QLIKE heavily penalises under-forecasting of large
    realised variance, which is exactly where the tree model is weakest.
</source_of_truth>

<read_first>
  - reports/ml_vs_baselines.md (the authoritative per-asset / per-regime numbers + the losses list)
  - reports/ml_vs_baselines.csv (provenance for the same numbers)
  - reports/baseline_eval.md (unit convention, QLIKE definition, walk-forward protocol)
  - .planning/phases/05-dashboard-honest-documentation/05-CONTEXT.md (required MODEL_CARD sections)
  - CLAUDE.md (intended-use framing: forecasting + MLOps demo, explicitly NOT a trading strategy)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author MODEL_CARD.md from the report files</name>
  <files>MODEL_CARD.md</files>
  <read_first>
    - reports/ml_vs_baselines.md (copy per-asset overall + per-regime numbers and the losses list verbatim)
    - reports/baseline_eval.md (QLIKE definition + walk-forward protocol + unit convention)
    - reports/ml_vs_baselines.csv (cross-check numbers / cite as provenance)
    - .planning/phases/05-dashboard-honest-documentation/05-CONTEXT.md (mandatory section list for DOCS-01)
  </read_first>
  <action>
    Write MODEL_CARD.md at the repo root for the registered model `volforecast-lgbm`
    (champion alias). Structure it as a standard model card with these sections, every metric
    copied verbatim from the source-of-truth report files (cite the source file inline):
    (1) Model details — name volforecast-lgbm, LightGBM 4.6 regressor, MLflow registry with
    @champion/@challenger aliases (not deprecated stages), target = next-period daily realized
    variance of decimal log returns (canonical definition from features/target.py);
    (2) Intended use — a volatility-forecasting + MLOps lifecycle DEMO; explicitly state it is
    NOT a trading strategy, NOT price-direction prediction, and not for live risk decisions
    (per CLAUDE.md / PROJECT.md out-of-scope);
    (3) Training data + window — BTC-USD, ETH-USD, SPY, AAPL, MSFT daily OHLCV ~2022-2026, free
    sources (ccxt/Binance + yfinance), DVC-versioned;
    (4) Evaluation methodology — leak-free purged walk-forward expanding window (min_train=252,
    step=21), identical folds for ML and all three baselines, vol terciles computed on test-fold
    realized variance only (no lookahead);
    (5) Metrics definitions — QLIKE (Patton 2011 variance form, =0 at perfect forecast, lower
    better), RMSE, MAE, all in daily decimal variance units;
    (6) Per-asset overall results table — LightGBM vs EWMA/GARCH/HAR RMSE/MAE/QLIKE for all 5
    assets (from Section 1 of ml_vs_baselines.md);
    (7) Per-regime breakdown — the per-vol-tercile (low/mid/high) table for each asset (from
    Section 2), and at minimum a representative per-calendar-year view (from Section 3);
    (8) Honest findings — a dedicated section that explicitly NAMES every asset/regime where
    LightGBM's QLIKE loses to the best classical baseline, with the deltas, reproducing
    Section 4 of ml_vs_baselines.md; lead with the high-vol QLIKE blowups (BTC/ETH/AAPL/MSFT/SPY
    high-vol tercile) and explain WHY (QLIKE penalises under-forecasting large realised variance;
    the tree model under-predicts vol spikes);
    (9) Assumptions — daily horizon, decimal-variance unit convention, RV proxy, free-data
    caveats (yfinance flakiness handled via cache + DVC);
    (10) Known limitations — including the documented v1 limitation carried from Phase 4 that the
    promotion cooldown is NOT persisted across Prefect runs (force_retrain is an ephemeral flow
    parameter; the performance flag is recomputed fresh per run), plus the QLIKE weakness above
    and the absence of intraday/true-RV (a v2 stretch).
    Do NOT fabricate any metric. Where a number is reproduced, it must match the report file
    exactly. Keep prose honest and plain — do not soften the losses.
  </action>
  <verify>
    <automated>uv run python -c "t=open('MODEL_CARD.md',encoding='utf-8').read(); assert 'QLIKE' in t; assert 'walk-forward' in t.lower(); assert 'not a trading strategy' in t.lower() or 'not a trading' in t.lower(); assert t.lower().count('lightgbm')>=5; assert 'GARCH' in t and 'EWMA' in t and 'HAR' in t; assert ('11.49' in t or '11.50' in t or '11.5' in t); print('model-card-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `MODEL_CARD.md` contains a per-asset overall comparison table covering LightGBM, EWMA, GARCH, HAR with RMSE/MAE/QLIKE
    - `MODEL_CARD.md` contains a per-regime (vol-tercile) breakdown table
    - `MODEL_CARD.md` contains an explicit section naming where ML loses to GARCH/EWMA/HAR (the high-vol QLIKE losses appear, e.g. the BTC-USD high-vol LightGBM-vs-GARCH number ~11.5 vs ~0.68)
    - `MODEL_CARD.md` states intended use as a forecasting/MLOps DEMO and explicitly NOT a trading strategy
    - `MODEL_CARD.md` defines QLIKE (Patton 2011 variance form, 0 at perfect forecast) and describes the leak-free purged walk-forward methodology (min_train=252, step=21)
    - `MODEL_CARD.md` lists the Phase-4 promotion-cooldown-not-persisted limitation under known limitations
    - Spot-check: at least three reproduced numbers match reports/ml_vs_baselines.md exactly
  </acceptance_criteria>
  <done>MODEL_CARD.md exists with per-asset + per-regime honest tables, a named-losses section, intended-use/methodology/metrics/assumptions/limitations sections, and every metric traceable to the report files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author → committed docs | A static Markdown doc is added to the repo and rendered on GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | MODEL_CARD.md leaks secrets | mitigate | The card contains only published evaluation metrics from committed report files plus methodology prose — no credentials, no API keys, no infra secrets. Reviewer must not paste env/.env values into the card. |
| T-05-06 | Repudiation | misrepresented/invented metrics | mitigate | Every number is copied verbatim from reports/ml_vs_baselines.{md,csv} and reports/baseline_eval.md and cross-checked; acceptance criteria spot-check three reproduced numbers against the source. Honesty (naming losses) is a hard requirement, not optional. |
</threat_model>

<verification>
- `MODEL_CARD.md` passes the automated content checks (QLIKE, walk-forward, not-a-trading-strategy, all three baselines named, high-vol QLIKE loss number present)
- Manual spot-check of three reproduced metrics against reports/ml_vs_baselines.md
</verification>

<success_criteria>
- A reviewer reads MODEL_CARD.md and comes away with an accurate, regime-segmented picture:
  where LightGBM helps (RMSE/MAE, low/mid vol) and where it clearly loses (QLIKE, high vol),
  with the methodology and limitations stated honestly and every number traceable to the
  committed reports.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-honest-documentation/05-02-SUMMARY.md` when done
</output>
