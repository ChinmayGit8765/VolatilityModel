---
phase: 01-foundation-validated-data
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/volforecast/ingest/crypto.py
  - src/volforecast/ingest/equity.py
  - src/volforecast/ingest/__init__.py
  - src/volforecast/cli.py
  - config/assets.yaml
  - tests/fixtures/equity_sample.parquet
  - tests/unit/test_ingest.py
autonomous: false
requirements: [INGEST-01, INGEST-02]
must_haves:
  truths:
    - "`volforecast ingest` (no --symbol) ingests every asset in config/assets.yaml: BTC, ETH (crypto) and SPY, AAPL, MSFT (equity)"
    - "Re-running ingest is cache-first incremental: it resumes from the last stored timestamp per asset and does not re-download the full history"
    - "Equity download uses explicit auto_adjust=True, threads=False, and tenacity retry/backoff on rate limits"
    - "Crypto ingestion supports a configurable exchange (Binance default, Kraken fallback) via config/assets.yaml"
  artifacts:
    - path: "src/volforecast/ingest/equity.py"
      provides: "yfinance batched equity OHLCV adapter with auto_adjust + retry"
      min_lines: 30
    - path: "config/assets.yaml"
      provides: "full asset universe: BTC, ETH, SPY, AAPL, MSFT"
      contains: "ETH"
    - path: "tests/fixtures/equity_sample.parquet"
      provides: "committed equity fixture for offline tests"
    - path: "tests/unit/test_ingest.py"
      provides: "incremental resume + per-asset dispatch + equity-adapter (mocked) tests"
      min_lines: 25
  key_links:
    - from: "src/volforecast/cli.py"
      to: "src/volforecast/ingest/equity.py"
      via: "ingest dispatch by asset_class"
      pattern: "from volforecast.ingest"
    - from: "src/volforecast/ingest/equity.py"
      to: "tenacity"
      via: "retry decorator on yfinance download"
      pattern: "retry"
---

<objective>
Broaden the skeleton's single-asset ingest path into the full cross-asset universe: BTC + ETH via ccxt and SPY + AAPL + MSFT via yfinance, all cache-first incremental. After this plan, `volforecast ingest` with no arguments fetches and stores 2+ years of daily OHLCV for all five assets, resuming from the last stored timestamp per asset on re-run.

Purpose: Deliver the multi-asset history the Phase 2 walk-forward harness needs, with the two real-world fragile integrations (ccxt incomplete candles, yfinance rate limits / auto_adjust) handled explicitly per the locked decisions.

Output: A yfinance equity adapter, the full asset universe config, an updated CLI that ingests all assets by default, and offline-fixture tests for incremental resume and per-asset dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-validated-data/01-CONTEXT.md
@.planning/phases/01-foundation-validated-data/01-RESEARCH.md
@.planning/phases/01-foundation-validated-data/SKELETON.md

<interfaces>
<!-- From Plan 01 (src/volforecast/ingest/base.py): -->
<!--   OHLCV_COLUMNS = ["open","high","low","close","volume"] -->
<!--   drop_incomplete_candles(candles, timeframe_ms, now_ms) -> list -->
<!--   candles_to_df(candles) -> DataFrame (tz-aware UTC DatetimeIndex "date", float cols) -->
<!--   incremental_update(existing_path, new_bars) -> DataFrame (merge-dedupe keep=last, writes parquet) -->
<!-- From Plan 01 (src/volforecast/config.py): -->
<!--   load assets from config/assets.yaml; symbol_slug(symbol); raw_path(asset) -> data/raw/{asset_class}/{slug}.parquet -->
<!-- Equity adapter output MUST conform to the same OHLCV contract (same columns, "date" index). -->

RESEARCH.md Pattern 3 (yfinance batch download with auto_adjust + tenacity retry) is the authoritative
implementation reference. The ccxt incremental-resume logic reuses base.incremental_update from Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Equity yfinance adapter (auto_adjust + retry) and equity fixture</name>
  <files>src/volforecast/ingest/equity.py, src/volforecast/ingest/__init__.py, tests/fixtures/equity_sample.parquet, tests/unit/test_ingest.py</files>
  <read_first>
    - src/volforecast/ingest/base.py (OHLCV contract, candles_to_df, incremental_update — must conform)
    - .planning/phases/01-foundation-validated-data/01-RESEARCH.md (Pattern 3 yfinance, Pitfall 6 auto_adjust, "Don't Hand-Roll" yfinance retry row, Open Question 1 nospam extra)
    - src/volforecast/config.py (symbol_slug, raw_path)
  </read_first>
  <behavior>
    - `download_equity_ohlcv(tickers, start, end)` is wrapped with a tenacity `@retry` (exponential backoff, stop_after_attempt, reraise=True), calls `yf.download(..., auto_adjust=True, threads=False, progress=False)`, and returns a per-ticker mapping of DataFrames each conforming to OHLCV_COLUMNS with a "date" index.
    - When `VOLFORECAST_NO_LIVE_API=1` the adapter must NOT call yfinance — the test injects a monkeypatched downloader so the unit test runs fully offline against `tests/fixtures/equity_sample.parquet`.
    - Test `test_equity_adapter_normalizes_to_ohlcv_contract`: given a monkeypatched yfinance return shaped like yf.download multi-ticker output, the adapter yields per-ticker DataFrames with exactly OHLCV_COLUMNS, float dtype, tz-aware UTC "date" index.
    - Test `test_equity_adapter_retries_then_succeeds`: a downloader that raises once then succeeds results in a successful return (proves the retry wrapper is wired).
  </behavior>
  <action>
    Create `tests/fixtures/equity_sample.parquet` (~15-20 daily rows, SPY-shaped, OHLCV contract). Implement `src/volforecast/ingest/equity.py` per RESEARCH Pattern 3: a module-level tenacity retry-decorated `download_equity_ohlcv(tickers, start, end)` (stop_after_attempt 5, wait_exponential multiplier 1 min 2 max 60, reraise True) calling `yf.download` with explicit auto_adjust=True, threads=False, progress=False (document the auto_adjust choice in a docstring per Pitfall 6); a `normalize_equity_frame(raw, ticker)` that reshapes yf's MultiIndex/single-index columns into the OHLCV contract (rename Open/High/Low/Close/Volume to lowercase, build "date" index tz-localized to UTC); guard live calls behind VOLFORECAST_NO_LIVE_API. Export the adapter from `ingest/__init__.py`. Write `tests/unit/test_ingest.py` with the two equity tests above, monkeypatching the downloader so no network is touched. Verify `requests-ratelimiter` is present after sync (RESEARCH Open Question 1) — if absent, add it explicitly to pyproject deps and note in SUMMARY.
  </action>
  <verify>
    <automated>cd "C:\bullshitprojects\VolatilityProj\VolatilityModel" && uv run pytest tests/unit/test_ingest.py -q && uv run python -c "import inspect, volforecast.ingest.equity as e; src=inspect.getsource(e); assert 'auto_adjust=True' in src and 'threads=False' in src and 'retry' in src, 'missing required yfinance flags or retry'; print('equity-adapter-ok')" && uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/equity_sample.parquet` exists and read-back conforms to OHLCV_COLUMNS with a "date" index.
    - `src/volforecast/ingest/equity.py` source contains `auto_adjust=True`, `threads=False`, and a tenacity retry decorator (verified by the inspect assertion).
    - Both equity tests pass with no network access (monkeypatched downloader).
    - `uv run ruff check src tests` exits 0.
  </acceptance_criteria>
  <done>Equity adapter conforms to the OHLCV contract with explicit auto_adjust/threads/retry, tested fully offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cache-first incremental + full asset universe + multi-asset CLI</name>
  <files>src/volforecast/ingest/crypto.py, src/volforecast/cli.py, config/assets.yaml, tests/unit/test_ingest.py</files>
  <read_first>
    - src/volforecast/ingest/base.py (incremental_update merge-dedupe contract)
    - src/volforecast/cli.py (current ingest subcommand from Plan 01)
    - .planning/phases/01-foundation-validated-data/01-RESEARCH.md (Pattern 2 ccxt since-pagination, "Incremental merge-dedupe" example, Pitfall 1 incomplete candle)
    - config/assets.yaml (single skeleton entry from Plan 01)
  </read_first>
  <behavior>
    - Test `test_incremental_resume_uses_last_stored_timestamp`: given an existing parquet ending at date D, the crypto ingest computes its since_ms from D (not from the configured start), so a re-run requests only bars after D rather than the full history.
    - Test `test_ingest_all_dispatches_by_asset_class`: `volforecast ingest` with no --symbol iterates every asset in config/assets.yaml, routing crypto assets to the ccxt adapter and equity assets to the yfinance adapter (both monkeypatched), writing one parquet per asset under the correct data/raw/{asset_class}/ path.
    - Re-running over an overlapping range produces no duplicate index entries (reuses base.incremental_update keep="last").
  </behavior>
  <action>
    Update `config/assets.yaml` to the full universe: BTC/USDT and ETH/USDT (asset_class crypto, exchange binance, with a documented kraken fallback field), and SPY, AAPL, MSFT (asset_class equity). Add a cache-first helper `resume_since_ms(existing_path, default_start)` (in crypto.py or a small shared function) that reads the last stored "date" from the existing parquet and returns the next-day ms, falling back to default_start when the file is absent. Wire `fetch_crypto_ohlcv` to accept this resumed since_ms. Update `cli.py`: when --symbol is omitted, the `ingest` subcommand loads all assets from config and dispatches each to `fetch_crypto_ohlcv` (crypto) or the equity adapter (equity) by asset_class, applies the validation gate (crypto schema for now — Plan 03 adds the equity schema and wires it), and writes via `incremental_update` to `raw_path(asset)`; keep the explicit single --symbol path working. Extend `tests/unit/test_ingest.py` with the two tests above, monkeypatching both adapters so the full dispatch runs offline.
  </action>
  <verify>
    <automated>cd "C:\bullshitprojects\VolatilityProj\VolatilityModel" && uv run pytest tests/unit/test_ingest.py -q && uv run python -c "import yaml; a=yaml.safe_load(open('config/assets.yaml')); items=a if isinstance(a,list) else a['assets']; syms={x['symbol'] for x in items}; assert {'BTC/USDT','ETH/USDT','SPY','AAPL','MSFT'} <= syms, syms; print('universe-ok', syms)" && uv run volforecast ingest --help && uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `config/assets.yaml` contains all five symbols (BTC/USDT, ETH/USDT, SPY, AAPL, MSFT) with asset_class and exchange fields; the universe assertion passes.
    - `resume_since_ms` returns a since value derived from the existing parquet's last date when present, and `default_start` otherwise (proven by `test_incremental_resume_uses_last_stored_timestamp`).
    - `volforecast ingest` with no --symbol dispatches crypto vs equity by asset_class and writes one parquet per asset (proven offline by `test_ingest_all_dispatches_by_asset_class`).
    - Overlapping re-run yields no duplicate index entries.
    - `uv run ruff check src tests` exits 0.
  </acceptance_criteria>
  <done>Full five-asset universe ingests with per-asset cache-first incremental resume and correct adapter dispatch, all proven offline on fixtures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Package legitimacy checkpoint (tenacity + requests-ratelimiter)</name>
  <files>pyproject.toml, uv.lock</files>
  <action>Pause for human verification of the two [ASSUMED]-tagged packages this plan relies on (tenacity, requests-ratelimiter). No code is written; the human confirms package legitimacy on PyPI and confirms the rate limiter imports, then approves before ingestion is trusted. This checkpoint is never auto-approvable.</action>
  <what-built>Two third-party packages required by ingestion are tagged [ASSUMED] in the RESEARCH Package Legitimacy Audit (slopcheck was unavailable): `tenacity` (yfinance retry/backoff — exercised by this plan's equity adapter) and the `requests-ratelimiter` dependency pulled by the `yfinance[nospam]` extra. This checkpoint verifies their legitimacy before relying on them. (`psycopg2-binary`, the other [ASSUMED] package, is verified in Plan 04 where the Postgres backend first exercises it.)</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/tenacity/ and confirm: source repo github.com/jd/tenacity, recent release history, multi-million weekly downloads.
    2. Open https://pypi.org/project/requests-ratelimiter/ and confirm a real maintained project (source repo present, non-trivial download count).
    3. Confirm the `yfinance[nospam]` extra actually installed the rate limiter: run `uv run python -c "import requests_ratelimiter; print(requests_ratelimiter.__version__)"` — it should print a version, not ImportError. If it fails, the executor must add `requests-ratelimiter` explicitly to pyproject.
    4. Reply "approved" if both packages are legitimate and the rate limiter imports, or describe what looks wrong.
  </how-to-verify>
  <verify><human-check>Human confirms tenacity and requests-ratelimiter are legitimate on PyPI and the rate limiter imports successfully.</human-check></verify>
  <resume-signal>Type "approved" to proceed, or describe the issue (e.g., missing rate limiter, suspicious package).</resume-signal>
  <done>Both [ASSUMED] ingestion packages are confirmed legitimate by the human and approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance API -> equity adapter | Untrusted equity OHLCV crosses here (rate limits, occasional fabricated weekend rows, adjusted vs unadjusted ambiguity) |
| public crypto exchange -> ccxt adapter | Untrusted crypto OHLCV (forming candles, gaps) |
| yfinance[nospam] extra -> environment | Transitive dependency (requests-ratelimiter) trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | yfinance auto_adjust ambiguity | mitigate | Explicit `auto_adjust=True` set and documented; ingestion records adjusted OHLC; verified by the inspect assertion in Task 1 |
| T-02-02 | Tampering | yfinance threading race (shared-global dict) | mitigate | `threads=False` enforced (RESEARCH anti-pattern); verified by inspect assertion |
| T-02-03 | Denial of Service | Yahoo / exchange rate limiting | mitigate | tenacity exponential backoff retry + yfinance[nospam] requests-ratelimiter; ccxt enableRateLimit |
| T-02-04 | Tampering | uv installs of [ASSUMED] packages (tenacity, requests-ratelimiter) | mitigate | Blocking human-verify checkpoint (Task 3) before relying on them; uv.lock hash-locks |
| T-02-05 | Tampering | fabricated weekend equity rows | transfer | Detected downstream by the equity calendar gate in Plan 03 (XNYS sessions); ingestion stores raw, validation rejects |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_ingest.py -q` passes all equity, incremental-resume, and dispatch tests fully offline (no live API).
- `config/assets.yaml` enumerates all five assets; the universe assertion passes.
- The equity adapter source contains the required auto_adjust/threads/retry constructs.
- `uv run ruff check src tests` exits 0.
- The blocking package-legitimacy checkpoint (Task 3) is approved before ingestion is trusted.
</verification>

<success_criteria>
- A single command ingests 2+ years of daily OHLCV for BTC, ETH (ccxt) and SPY, AAPL, MSFT (yfinance) with per-asset cache-first incremental resume and incomplete-last-candle handling (INGEST-01 complete; INGEST-02 complete).
- Crypto exchange is configurable (Binance default, Kraken fallback) via config/assets.yaml.
- Equity ingestion uses explicit auto_adjust=True and tolerates rate limits via tenacity + nospam.
- No live API calls occur in tests — all coverage is fixture/monkeypatch based.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-validated-data/01-02-SUMMARY.md` when done.
</output>
