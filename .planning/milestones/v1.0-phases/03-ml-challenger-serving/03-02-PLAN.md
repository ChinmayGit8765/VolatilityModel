---
phase: 03-ml-challenger-serving
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/volforecast/models/lgbm.py
  - scripts/train_lgbm.py
  - tests/unit/test_shap_artifacts.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]
must_haves:
  truths:
    - "LightGBM trains pooled across all 5 assets on log-variance, evaluated on the identical walk_forward_splits used by the Phase 2 baselines"
    - "Hyperparameters are selected on inner validation folds only — test folds are never touched during grid search"
    - "Per-asset RMSE/MAE/QLIKE are computed on the variance scale with the canonical eval/metrics.py functions"
    - "A training run appears in MLflow with params, per-asset metrics, data_hash + git_sha tags, and a registered model version carrying the @champion alias"
    - "SHAP global bar + beeswarm PNGs exist as MLflow artifacts under the registered run"
  artifacts:
    - path: "src/volforecast/models/lgbm.py"
      provides: "grid search on inner val folds, train_pooled_model, evaluate_per_asset, compute_shap_artifacts"
      exports: ["PARAM_GRID", "train_pooled_model", "evaluate_per_asset", "compute_shap_artifacts"]
    - path: "scripts/train_lgbm.py"
      provides: "MLflow run + registry alias entry point"
      contains: "set_registered_model_alias"
    - path: "tests/unit/test_shap_artifacts.py"
      provides: "SHAP shape-check coverage (offline)"
  key_links:
    - from: "scripts/train_lgbm.py"
      to: "mlflow registry volforecast-lgbm@champion"
      via: "MlflowClient.set_registered_model_alias"
      pattern: "set_registered_model_alias"
    - from: "src/volforecast/models/lgbm.py::evaluate_per_asset"
      to: "src/volforecast/eval/metrics.py::qlike"
      via: "variance-scale scoring"
      pattern: "qlike\\("
---

<objective>
Train the pooled LightGBM challenger on the Phase 2 feature set, evaluate it per-asset on the identical walk-forward folds, track the run in the compose-stack MLflow, register it as `volforecast-lgbm` with the `@champion` alias, and log SHAP explainability artifacts.

Purpose: This is the modeling heart of Phase 3 — MODEL-01 (honest same-folds evaluation), MODEL-02 (MLflow tracking + alias registry, no deprecated stages), MODEL-03 (SHAP). It produces the registered champion that the serving container (Plan 04) loads and the comparison data the report (Plan 03) consumes.
Output: `scripts/train_lgbm.py` entry point; `models/lgbm.py` extended with grid search, training, evaluation, and SHAP; a registered `volforecast-lgbm@champion` MLflow model version with metrics, tags, and SHAP PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ml-challenger-serving/03-RESEARCH.md
@.planning/phases/03-ml-challenger-serving/03-CONTEXT.md

<environment>
- Windows 11; uv NOT on PATH — prepend in fresh shells:
  `export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"`
- Compose stack is UP. Training runs on the HOST via uv and sets `MLFLOW_TRACKING_URI=http://localhost:5000`. After Plan 01's `--serve-artifacts` change, host-side `log_model` uploads artifacts over HTTP (no volume mount needed).
- data/features/{crypto,equity}/{slug}.parquet (5 assets, 19 cols) and data/processed/{...}.parquet already exist locally (Phase 2 output, DVC-tracked). If missing: `uv run python scripts/generate_features.py`.
- GARCH-heavy work is already done in Phase 2; LightGBM training is fast (~5000 rows x ~19 cols, CPU).
- MLflow-dependent tests must NOT require the compose stack: mock MLflow with unittest.mock or point `mlflow.set_tracking_uri` at a tmp file:// dir. CI has no compose stack.
</environment>

<interfaces>
<!-- From Plan 03-01 (this phase, already built). Use directly. -->
From src/volforecast/models/lgbm.py:
```python
LOG_VAR_EPS: float = 1e-10
KNOWN_ASSETS: list[str] = ["BTC-USD", "ETH-USD", "SPY", "AAPL", "MSFT"]
ASSET_DTYPE: pd.CategoricalDtype  # categories=KNOWN_ASSETS, ordered=False
def to_log_var(var) -> np.ndarray
def from_log_var(log_var) -> np.ndarray
def assemble_pooled_train(asset_feature_dfs, asset_target_series, fold_i,
                          min_train=252, step=21, horizon=1) -> tuple[pd.DataFrame, pd.Series]
    # returns pooled (X with category "asset" col, y log-var target); test_idx rows excluded
```

From src/volforecast/eval/harness.py and metrics.py (Phase 2):
```python
def walk_forward_splits(n, min_train=252, step=21, horizon=1) -> Generator[WalkForwardSplit]
def qlike(rv_var, forecast_var) -> float   # variance scale; qlike(x,x)==0
def rmse(y_true, y_pred) -> float
def mae(y_true, y_pred) -> float
```

From src/volforecast/features/{target,pipeline}.py and config.py:
```python
HORIZON: int = 1
def compute_target(close: pd.Series, horizon=HORIZON) -> pd.Series  # NaN tail; never fill 0
def build_features(df, cross_asset_dfs=None, include_garch=True) -> pd.DataFrame  # 19-col, no "asset" col
def load_assets(config_path=None) -> list[dict]      # keys: symbol, asset_class
def processed_path(asset, data_root=None) -> Path
def symbol_slug(symbol) -> str                       # "BTC/USDT" -> "BTC-USD"
```

MLflow 3.13 alias API (verified — RESEARCH Pattern 3):
```python
mlflow.set_tracking_uri("http://localhost:5000")
model_info = mlflow.lightgbm.log_model(lgb_model=..., name="model",
              registered_model_name="volforecast-lgbm", signature=..., input_example=...)
client = MlflowClient()
client.set_registered_model_alias("volforecast-lgbm", "champion", model_info.registered_model_version)
client.set_model_version_tag("volforecast-lgbm", version, "validation_status", "passed")
mv = client.get_model_version_by_alias("volforecast-lgbm", "champion")  # round-trip check
```

The published bar to beat (reports/baseline_metrics.csv): per-asset EWMA/GARCH/HAR RMSE/MAE/QLIKE
(e.g. BTC HAR qlike 1.9307; SPY EWMA qlike 1.6344). HAR-RV is best QLIKE on 4/5 assets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grid search on inner validation folds + pooled training + per-asset evaluation</name>
  <files>src/volforecast/models/lgbm.py</files>
  <read_first>
    - src/volforecast/models/lgbm.py (the Plan 01 contracts already present)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Pattern 7 LGBMRegressor params + eval_set + early_stopping; Pitfall 2 inner-validation-only; Open Question #2 — use last 3 inner folds; Code Examples PARAM_GRID + infer_signature)
    - .planning/phases/03-ml-challenger-serving/03-CONTEXT.md (ML Training Design locked decisions: pooled, log-variance, ~20 combos, val folds only, fixed seed)
    - reports/baseline_metrics.csv (per-asset bar for sanity comparison)
  </read_first>
  <action>
    Extend `src/volforecast/models/lgbm.py` (do NOT create a new module). Implement per MODEL-01 and the CONTEXT locked decisions:
    Define `PARAM_GRID` as a small dict producing ~20 combos via itertools.product over num_leaves/min_child_samples/learning_rate/reg_lambda/feature_fraction (exact combos are Claude's discretion per CONTEXT; keep near 20). Use `random_state=42`, `n_estimators=1000` with `early_stopping(stopping_rounds=50)` and the 4.6.0 `eval_set=` API (NOT eval_X/eval_y — pinned at 4.6.0). Mark the `"asset"` column as ASSET_DTYPE and pass `categorical_feature="auto"`.
    Add `train_pooled_model(asset_feature_dfs, asset_target_series, params, fold_i, ...)` that assembles the pooled train set via `assemble_pooled_train`, carves an INNER validation window from the tail of each asset's train split (last 3 inner folds per Open Question #2 — positions strictly inside the train window, never the test fold; Pitfall 2), fits LGBMRegressor on log-variance target with that inner val as `eval_set`, and returns the fitted model + best_iteration_.
    Add `grid_search(asset_feature_dfs, asset_target_series, param_grid, ...)` that scores each combo by average QLIKE on the INNER validation folds only (predictions inverse-transformed via from_log_var before qlike), and returns the best params dict. Test folds MUST NOT be referenced anywhere in grid_search.
    Add `evaluate_per_asset(model, asset_feature_dfs, asset_target_series, ...)` that walks each asset's `walk_forward_splits` test folds, predicts on the test rows (asset col cast to ASSET_DTYPE), applies from_log_var, and computes per-asset rmse/mae/qlike on the VARIANCE scale using eval/metrics.py against compute_target-derived realized variance. Return `{symbol: {"rmse":..,"mae":..,"qlike":..,"n_forecasts":..}}`.
    No MLflow calls in this task — pure modeling functions (Task 2 wires MLflow).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run python -c "from volforecast.models.lgbm import PARAM_GRID, train_pooled_model, grid_search, evaluate_per_asset; import itertools; n=len(list(itertools.product(*PARAM_GRID.values()))); print('combos', n); assert 12 <= n <= 32"</automated>
  </verify>
  <acceptance_criteria>
    - PARAM_GRID expands to between 12 and 32 combos (near the ~20 target).
    - `train_pooled_model`, `grid_search`, `evaluate_per_asset` importable; grid_search source contains no reference to test_idx (Pitfall 2 — inner-val only).
    - `uv run ruff check src/volforecast/models/lgbm.py` clean.
    - A scratch run on the 5 real feature parquets produces a per-asset metrics dict with all 5 symbols and finite QLIKE values (sanity: same order of magnitude as baseline_metrics.csv, ~1.6–2.1).
  </acceptance_criteria>
  <done>Pooled LightGBM trains on log-variance with inner-validation-only grid search; per-asset variance-scale RMSE/MAE/QLIKE computed on identical folds via canonical metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Train script — MLflow run, registry, @champion alias, data lineage tags</name>
  <files>scripts/train_lgbm.py, src/volforecast/models/lgbm.py</files>
  <read_first>
    - scripts/generate_features.py (the load-assets + per-asset parquet-loading + sys.path pattern to mirror)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Pattern 3 MLflow alias workflow; Code Examples infer_signature; State of the Art — explicit log over autolog)
    - .planning/phases/03-ml-challenger-serving/03-CONTEXT.md (MLflow Tracking & Registry decisions: model name volforecast-lgbm, @champion to best run, data hash logged)
  </read_first>
  <action>
    Create `scripts/train_lgbm.py` runnable as `uv run python scripts/train_lgbm.py` from project root (mirror generate_features.py's sys.path + load_assets + processed/feature parquet loading). Steps:
    1. Load the 5 feature parquets (data/features/...) into `asset_feature_dfs` and derive `asset_target_series` per asset via `compute_target` on the processed close prices (align indices; drop NaN tail). Read `MLFLOW_TRACKING_URI` from env (default `http://localhost:5000`); call `mlflow.set_tracking_uri(...)`.
    2. Run `grid_search` to pick best params (inner-val only), then `train_pooled_model` on the full train window with best params to get the final model.
    3. Open `mlflow.start_run(run_name="lgbm-pooled-v1")`: `log_params(best_params)`; `log_metrics` of per-asset rmse/qlike/mae from `evaluate_per_asset` (prefix keys with symbol, e.g. `BTC-USD_qlike`); `set_tags({"data_hash": <md5 of the 5 feature parquets>, "git_sha": <current sha>, "train_window_end": <date>})`. Build `signature = infer_signature(X_val_sample, model.predict(X_val_sample))` and call `mlflow.lightgbm.log_model(lgb_model=model, name="model", registered_model_name="volforecast-lgbm", signature=signature, input_example=X_val_sample.head(3))`.
    4. After the run: `MlflowClient().set_registered_model_alias("volforecast-lgbm", "champion", model_info.registered_model_version)`, then `set_model_version_tag(..., "validation_status", "passed")`, then `get_model_version_by_alias("volforecast-lgbm", "champion")` and print version + run_id (round-trip verification). NO deprecated `transition_model_version_stage` anywhere.
    Add a small helper in models/lgbm.py for the feature-parquet data hash if it keeps the script clean (Claude's discretion).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run python scripts/train_lgbm.py && uv run python -c "import mlflow; mlflow.set_tracking_uri('http://localhost:5000'); from mlflow import MlflowClient; mv=MlflowClient().get_model_version_by_alias('volforecast-lgbm','champion'); print('champion version', mv.version, 'run', mv.run_id)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/train_lgbm.py` completes and registers a `volforecast-lgbm` version.
    - `get_model_version_by_alias("volforecast-lgbm","champion")` resolves to a concrete version with a run_id.
    - The MLflow run contains params, per-asset metric keys for all 5 symbols, and tags `data_hash`, `git_sha`, `train_window_end`.
    - `grep -rc transition_model_version_stage scripts/train_lgbm.py src/volforecast/models/lgbm.py` returns 0 (no deprecated stage API).
    - `uv run ruff check scripts/train_lgbm.py` clean.
  </acceptance_criteria>
  <done>A reproducible training run is tracked in MLflow and the best model is registered as volforecast-lgbm@champion with full lineage tags and metrics; no deprecated stages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SHAP TreeExplainer artifacts logged to the registered run</name>
  <files>src/volforecast/models/lgbm.py, scripts/train_lgbm.py, tests/unit/test_shap_artifacts.py</files>
  <read_first>
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Pattern 4 SHAP TreeExplainer; Pitfall 6 — explain the native LGBM model, NOT the pyfunc wrapper; SHAP 0.52 + numpy2 compatibility)
    - .planning/phases/03-ml-challenger-serving/03-CONTEXT.md (SHAP & Report: global bar + beeswarm PNGs, per-asset top-10 importances, logged under the registered run)
  </read_first>
  <behavior>
    - compute_shap_artifacts on a fitted LGBMRegressor over X_reference (n rows x f features) returns SHAP values whose shape is (n, f)
    - it writes a global bar PNG and a beeswarm PNG to the output dir; both files exist and are non-empty
    - per-asset top-10: filtering X_reference to one asset's rows and recomputing yields a ranked feature list of length <= 10
    - it is called with the native LGBMRegressor object (never an mlflow pyfunc wrapper) — Pitfall 6
  </behavior>
  <action>
    Add `compute_shap_artifacts(model, X_reference, feature_names, output_dir)` to models/lgbm.py per Pattern 4: build `shap.TreeExplainer(model)` on the native fitted LGBMRegressor (the same object trained in Task 1 — NOT a loaded pyfunc; Pitfall 6), compute `shap_values`, save a global `plot_type="bar"` summary PNG and a beeswarm summary PNG with matplotlib (Agg backend, `show=False`, savefig + close). Return a dict of file paths plus per-asset top-10 feature names (filter X_reference rows by the `asset` category column, recompute mean |shap| ranking). Wire it into `scripts/train_lgbm.py` INSIDE the active `mlflow.start_run` (before run close), using the test-fold rows as X_reference, and `mlflow.log_artifact(path, artifact_path="shap")` for each PNG. SHAP is fast on ~5000x19 — no batching.
    Create `tests/unit/test_shap_artifacts.py` covering the <behavior> shape + file-existence cases on a tiny synthetic LGBMRegressor trained inline (offline, no MLflow, tmp_path for outputs).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run pytest tests/unit/test_shap_artifacts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_shap_artifacts.py -x -q` passes (shape (n,f); both PNGs exist non-empty; top-10 length <= 10).
    - After re-running `scripts/train_lgbm.py`, the registered run's artifacts include `shap/shap_global_bar.png` and `shap/shap_beeswarm.png` (verify via `MlflowClient().list_artifacts(run_id, "shap")`).
    - SHAP is computed on the native LGBMRegressor, not a pyfunc wrapper (source review: TreeExplainer arg is the fitted model object).
    - `uv run ruff check src/volforecast/models/lgbm.py scripts/train_lgbm.py tests/unit/test_shap_artifacts.py` clean.
  </acceptance_criteria>
  <done>SHAP global + beeswarm PNGs and per-asset top-10 importances are produced at training time from the native model and logged as MLflow artifacts under the registered champion run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host training script → MLflow server (HTTP, loopback) | Run params, metrics, model artifact, SHAP PNGs cross |
| feature parquet files → training input | Trusted local DVC-tracked data; hashed for lineage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Registered model provenance | mitigate | `data_hash` + `git_sha` lineage tags on the run; `validation_status=passed` version tag — provenance auditable before any alias-based load |
| T-03-04 | Repudiation | Training reproducibility | mitigate | Fixed `random_state=42`, params + data_hash logged to MLflow — runs are reproducible and attributable |
| T-03-05 | Information Disclosure | MLflow run/metrics on loopback server | accept | localhost:5000 only, no LAN exposure; portfolio-local scope (RESEARCH security domain V2/V4 N/A) |
</threat_model>

<verification>
- `uv run python scripts/train_lgbm.py` registers volforecast-lgbm@champion with metrics, tags, and SHAP artifacts.
- `uv run pytest tests/unit/ -x -q` green (transforms, folds from Plan 01; shap from this plan).
- No deprecated MLflow stage API used.
- `uv run ruff check .` clean.
</verification>

<success_criteria>
- MODEL-01: pooled LightGBM evaluated per-asset on identical walk_forward_splits, variance-scale metrics via eval/metrics.py.
- MODEL-02: run tracked in MLflow; model registered as volforecast-lgbm with @champion alias (no stages).
- MODEL-03: SHAP global bar + beeswarm + per-asset top-10 logged as run artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-ml-challenger-serving/03-02-SUMMARY.md` when done.
</output>
