---
phase: 03-ml-challenger-serving
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - infra/mlflow-entrypoint.sh
  - src/volforecast/models/lgbm.py
  - tests/unit/test_lgbm_transforms.py
  - tests/unit/test_lgbm_folds.py
autonomous: true
requirements: [MODEL-01]
must_haves:
  truths:
    - "lightgbm, shap, fastapi, uvicorn import successfully under the Python 3.12 uv env"
    - "MLflow server serves artifacts over HTTP so host-side training and the serving container never need a direct artifact-volume mount"
    - "Variance round-trips through log space (from_log_var(to_log_var(v)) == v within tolerance) with a documented epsilon floor"
    - "Pooled fold assembly takes train rows from each asset's own walk_forward_splits and never includes any asset's test-fold positions"
  artifacts:
    - path: "src/volforecast/models/lgbm.py"
      provides: "log-variance transforms, KNOWN_ASSETS/ASSET_DTYPE constants, assemble_pooled_train fold-assembly"
      min_lines: 80
      exports: ["to_log_var", "from_log_var", "assemble_pooled_train", "KNOWN_ASSETS", "ASSET_DTYPE", "LOG_VAR_EPS"]
    - path: "tests/unit/test_lgbm_transforms.py"
      provides: "log-variance round-trip + epsilon floor coverage"
    - path: "tests/unit/test_lgbm_folds.py"
      provides: "pooled fold no-leakage invariant coverage"
    - path: "infra/mlflow-entrypoint.sh"
      provides: "MLflow server in artifact-proxy mode"
      contains: "--serve-artifacts"
  key_links:
    - from: "src/volforecast/models/lgbm.py::assemble_pooled_train"
      to: "src/volforecast/eval/harness.py::walk_forward_splits"
      via: "per-asset split generation"
      pattern: "walk_forward_splits"
---

<objective>
Lay the Phase 3 foundation: add the four new pinned dependencies, fix the MLflow artifact-access topology so host-side training can log artifacts over HTTP, and create the `models/lgbm.py` contract module (log-variance transforms, asset-categorical constants, leak-free pooled fold assembly) with the two unit-test scaffolds that downstream training depends on.

Purpose: Every Phase 3 plan imports from `models/lgbm.py`. Defining the transforms, the `KNOWN_ASSETS`/`ASSET_DTYPE` constants, and the pooled fold-assembly contract first prevents training-time and serving-time skew (Pitfall 3) and cross-asset leakage (Pitfall 1). The `--serve-artifacts` entrypoint change resolves the highest-risk infra detail (Research Open Question #1 / A4) before any `log_model` call runs.
Output: Updated dependency matrix, artifact-proxy MLflow server, `models/lgbm.py` with the core contracts, two passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ml-challenger-serving/03-RESEARCH.md

<environment>
- Windows 11; uv is NOT on PATH. Before any uv command in a fresh shell, prepend:
  `export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"`
- The compose stack is currently UP (MLflow 127.0.0.1:5000, Postgres 5433, Prefect 4200).
- After editing infra/mlflow-entrypoint.sh, the mlflow-server container must be recreated for the flag to take effect:
  `docker compose -f infra/docker-compose.yml up -d --force-recreate mlflow-server`
- The entrypoint script MUST keep LF line endings (.gitattributes enforces `*.sh eol=lf`); a CRLF entrypoint silently dies in the container.
</environment>

<interfaces>
<!-- Existing contracts the executor builds against. Use these directly; no codebase exploration needed. -->

From src/volforecast/eval/harness.py:
```python
@dataclass
class WalkForwardSplit:
    train_idx: np.ndarray   # contiguous positions [0..train_end-1]
    test_idx: np.ndarray    # contiguous positions starting >= train_idx.max()+horizon

def walk_forward_splits(n: int, min_train: int = 252, step: int = 21,
                        horizon: int = 1) -> Generator[WalkForwardSplit, None, None]:
    # Per-asset, integer-position splits. Invariant: max(train_idx) < min(test_idx),
    # min(test_idx) - max(train_idx) >= horizon. Stateless and deterministic.
```

From src/volforecast/eval/metrics.py:
```python
QLIKE_FLOOR: float = 1e-10            # reuse as the log-variance epsilon for consistency
def qlike(rv_var, forecast_var) -> float   # Patton variance form; qlike(x,x)==0; floors both inputs
def rmse(y_true, y_pred) -> float
def mae(y_true, y_pred) -> float
```

From src/volforecast/features/target.py:
```python
HORIZON: int = 1
def compute_target(close: pd.Series, horizon: int = HORIZON) -> pd.Series
    # target[t] = (ln(close[t+1]/close[t]))**2 ; last `horizon` rows NaN; never fill NaN with 0
```

From src/volforecast/features/pipeline.py:
```python
def build_features(df, cross_asset_dfs=None, include_garch=True) -> pd.DataFrame
    # 17/19-col leak-free matrix; index is UTC DatetimeIndex named "date".
    # Does NOT add an "asset" column — the training script adds it post-hoc.
```

From infra/mlflow-entrypoint.sh (current — to be changed):
```bash
exec mlflow server \
  --backend-store-uri "${MLFLOW_BACKEND_STORE_URI}" \
  --default-artifact-root /mlflow/artifacts \
  --host 0.0.0.0 --port 5000 --allowed-hosts '*'
```

Known asset universe (5): BTC-USD, ETH-USD, SPY, AAPL, MSFT (config/assets.yaml; slugs match data/features/{crypto,equity}/{slug}.parquet).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 dependencies and switch MLflow to artifact-proxy mode</name>
  <files>pyproject.toml, infra/mlflow-entrypoint.sh</files>
  <read_first>
    - pyproject.toml (the `[project].dependencies` list — confirm mlflow/pandas/numpy/pyarrow already present, do NOT duplicate)
    - infra/mlflow-entrypoint.sh (current exec line)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Standard Stack table; Pitfall 4; Assumptions Log A4 + note; Open Question #1 recommendation)
  </read_first>
  <action>
    Add exactly four pinned entries to `[project].dependencies` in pyproject.toml: `lightgbm==4.6.0`, `shap==0.52.0`, `fastapi==0.136.3`, `uvicorn[standard]==0.49.0`. Do NOT re-add mlflow/pandas/numpy/pyarrow (already pinned). These four packages are [ASSUMED]-tagged in the Package Legitimacy Audit and all approved (canonical Microsoft/shap/tiangolo/encode projects, 6+ year histories) — no install-time human checkpoint required.
    Edit infra/mlflow-entrypoint.sh: replace the single `--default-artifact-root /mlflow/artifacts` flag with `--artifacts-destination /mlflow/artifacts --serve-artifacts` (Research Open Question #1 recommended resolution / A4). This puts the tracking server in artifact-proxy mode so host-side training uploads artifacts over HTTP and the serving container does NOT need to mount the `mlflow_artifacts` volume. Keep all other flags (`--backend-store-uri`, `--host 0.0.0.0`, `--port 5000`, `--allowed-hosts '*'`) and LF line endings unchanged.
    Run `uv sync` (after exporting the uv PATH per <environment>) to resolve and lock the new deps. Recreate the mlflow-server container so the new entrypoint flag is live: `docker compose -f infra/docker-compose.yml up -d --force-recreate mlflow-server`.
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run python -c "import lightgbm, shap, fastapi, uvicorn; print(lightgbm.__version__, shap.__version__, fastapi.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import lightgbm, shap, fastapi, uvicorn"` exits 0 and prints versions 4.6.0 / 0.52.0 / 0.136.3.
    - `grep -v '^#' infra/mlflow-entrypoint.sh | grep -c -- '--serve-artifacts'` returns 1 and `grep -c -- '--default-artifact-root' infra/mlflow-entrypoint.sh` returns 0 (flag fully replaced).
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/health` returns 200 after the container recreate (server still healthy in proxy mode).
    - uv.lock updated and includes lightgbm 4.6.0.
  </acceptance_criteria>
  <done>Four deps installed and importable, MLflow server running in `--serve-artifacts` proxy mode and reachable at localhost:5000.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create models/lgbm.py log-variance transforms and asset-categorical constants</name>
  <files>src/volforecast/models/lgbm.py, tests/unit/test_lgbm_transforms.py</files>
  <read_first>
    - src/volforecast/eval/metrics.py (QLIKE_FLOOR = 1e-10 — reuse this exact value as LOG_VAR_EPS)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Pattern 2 log-variance; Pattern 7 category dtype; Pitfall 3; Pitfall 5)
    - config/assets.yaml (the 5-symbol universe ordering)
  </read_first>
  <behavior>
    - to_log_var(v): for v above LOG_VAR_EPS, from_log_var(to_log_var(v)) recovers v within rtol 1e-9
    - to_log_var(0.0) and to_log_var(negative): no inf/nan — floored at LOG_VAR_EPS, returns log(LOG_VAR_EPS)
    - from_log_var is exactly np.exp; round-trip on a vector of realistic variances (1e-5..1e-2) holds
    - ASSET_DTYPE is a pd.CategoricalDtype over the exact KNOWN_ASSETS list, ordered=False
    - applying ASSET_DTYPE to a Series of valid symbols yields no NaN categories; an unknown symbol becomes a NaN category (documented behavior)
  </behavior>
  <action>
    Create `src/volforecast/models/lgbm.py`. Define module constant `LOG_VAR_EPS: float = 1e-10` (import or mirror QLIKE_FLOOR from eval/metrics.py — document that they are intentionally equal per Pattern 2). Define `to_log_var(var)` returning `np.log(np.maximum(var, LOG_VAR_EPS))` and `from_log_var(log_var)` returning `np.exp(log_var)`, both accepting array-like and returning np.ndarray; docstring on from_log_var must note the Jensen's-inequality geometric-mean caveat (Pitfall 5) and that NO lognormal bias correction is applied. Define `KNOWN_ASSETS: list[str]` as the 5 slugs in config order (`["BTC-USD", "ETH-USD", "SPY", "AAPL", "MSFT"]`) and `ASSET_DTYPE = pd.CategoricalDtype(categories=KNOWN_ASSETS, ordered=False)` — these are the single source of truth applied identically at training and serving time (Pitfall 3 mitigation).
    Create `tests/unit/test_lgbm_transforms.py` implementing the <behavior> cases. Offline, no MLflow, no fixtures beyond inline arrays.
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run pytest tests/unit/test_lgbm_transforms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_lgbm_transforms.py -x -q` passes all cases.
    - `uv run python -c "from volforecast.models.lgbm import to_log_var, from_log_var, KNOWN_ASSETS, ASSET_DTYPE, LOG_VAR_EPS; assert LOG_VAR_EPS==1e-10; assert KNOWN_ASSETS==['BTC-USD','ETH-USD','SPY','AAPL','MSFT']; print('ok')"` prints ok.
    - `uv run ruff check src/volforecast/models/lgbm.py tests/unit/test_lgbm_transforms.py` clean.
  </acceptance_criteria>
  <done>Transforms round-trip with documented epsilon floor; asset categorical constants exported and used as the single source of truth for both train and serve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement leak-free pooled fold assembly with no-leakage unit test</name>
  <files>src/volforecast/models/lgbm.py, tests/unit/test_lgbm_folds.py</files>
  <read_first>
    - src/volforecast/eval/harness.py (walk_forward_splits contract; WalkForwardSplit integer-position semantics)
    - .planning/phases/03-ml-challenger-serving/03-RESEARCH.md (Pattern 1 pooled fold assembly; Pitfall 1 cross-asset leakage; Pitfall 2 test-folds-for-tuning)
    - src/volforecast/features/target.py (compute_target — NaN tail rows must be dropped before pooling)
  </read_first>
  <behavior>
    - assemble_pooled_train returns (X, y) whose row count equals the sum over assets of len(that asset's fold_i train_idx) minus rows with NaN target
    - For a synthetic two-asset case where asset B has a known test_idx range at fold_i, NO row whose position is in B's test_idx appears in the pooled X for that asset (compare against an "asset" + original-position tag)
    - Each pooled X carries an "asset" column equal to the source symbol; after astype(ASSET_DTYPE) there are no NaN categories
    - When fold_i exceeds an asset's number of splits, that asset contributes zero rows (skipped, no error)
    - NaN-target rows (the final horizon rows from compute_target) are excluded from the pool
  </behavior>
  <action>
    Add `assemble_pooled_train(asset_feature_dfs, asset_target_series, fold_i, min_train=252, step=21, horizon=1)` to `src/volforecast/models/lgbm.py` per Pattern 1. For each `(symbol, feat_df)`: generate `list(walk_forward_splits(len(feat_df), min_train, step, horizon))`; if `fold_i >= len(splits)` skip the asset; else take `split.train_idx` (integer positions in THAT asset's frame — never a date-range slice; Pitfall 1), select `feat_df.iloc[train_idx]`, add an `"asset"` column = symbol, take the aligned `target_series.iloc[train_idx]`, drop rows where target is NaN, and append. Concat all parts into one (X, y). Cast the pooled `"asset"` column to `ASSET_DTYPE` before returning X. Document that test_idx rows are structurally excluded because only train_idx positions are selected from each asset independently. Do NOT implement grid search or training here (Plan 02) — this task delivers only the assembly primitive and its leakage proof.
    Create `tests/unit/test_lgbm_folds.py` implementing the <behavior> cases with small synthetic per-asset DataFrames (no real parquet, offline).
  </action>
  <verify>
    <automated>export PATH="$PATH:/c/Users/chinm/AppData/Local/Programs/Python/Python313/Scripts:/c/Users/chinm/.local/bin"; uv run pytest tests/unit/test_lgbm_folds.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_lgbm_folds.py -x -q` passes, including an explicit assertion that no test-fold position from any asset appears in the pooled training rows.
    - `uv run python -c "from volforecast.models.lgbm import assemble_pooled_train; print('ok')"` prints ok.
    - `uv run pytest tests/unit/ -x -q` (full unit suite) stays green — no regression to the 276 existing tests.
    - `uv run ruff check src/volforecast/models/lgbm.py tests/unit/test_lgbm_folds.py` clean.
  </acceptance_criteria>
  <done>Pooled fold assembly reuses walk_forward_splits per asset, structurally excludes every asset's test-fold rows, and the no-leakage invariant is asserted by a passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host training script → MLflow server (HTTP) | Artifact + run data crosses; now via `--serve-artifacts` HTTP proxy on loopback |
| PyPI → uv install | New third-party package code enters the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | MLflow artifact root config change | mitigate | `--serve-artifacts` keeps artifacts in the existing `mlflow_artifacts` named volume (no new exposure); server still bound to `127.0.0.1:5000` only |
| T-03-02 | Information Disclosure | MLflow tracking server | accept | No auth on local loopback-only port; matches existing stack posture; local single-machine portfolio scope (V2/V4 N/A per RESEARCH security domain) |
| T-03-SC | Tampering | uv installs of lightgbm/shap/fastapi/uvicorn | mitigate | Package Legitimacy Audit: all four [ASSUMED] + Approved (canonical Microsoft/shap/tiangolo/encode repos, 6+ yr history, pinned exact versions). No [SUS]/[SLOP] packages → no blocking checkpoint required. |
</threat_model>

<verification>
- All four new deps importable under uv Python 3.12.
- MLflow server in proxy mode, healthy at localhost:5000.
- `uv run pytest tests/unit/ -x -q` green (new transform + fold tests pass; no regression).
- `uv run ruff check .` clean.
</verification>

<success_criteria>
- pyproject.toml pins lightgbm/shap/fastapi/uvicorn at researched versions; uv.lock updated.
- infra/mlflow-entrypoint.sh runs the server with `--serve-artifacts` (artifact-proxy mode).
- models/lgbm.py exports to_log_var, from_log_var, LOG_VAR_EPS, KNOWN_ASSETS, ASSET_DTYPE, assemble_pooled_train.
- Two unit tests (transforms round-trip, pooled fold no-leakage) pass.
</success_criteria>

<output>
Create `.planning/phases/03-ml-challenger-serving/03-01-SUMMARY.md` when done.
</output>
