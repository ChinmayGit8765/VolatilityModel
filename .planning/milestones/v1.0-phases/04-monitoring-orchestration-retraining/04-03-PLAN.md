---
phase: 04-monitoring-orchestration-retraining
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/volforecast/monitoring/performance.py
  - src/volforecast/monitoring/alerts.py
  - tests/unit/test_performance.py
  - tests/unit/test_alerts.py
autonomous: true
requirements: [MON-03, MON-04]
must_haves:
  truths:
    - "A rolling 21-day QLIKE of the champion is compared against the rolling 21-day QLIKE of the GARCH baseline over forecast_vs_realized rows"
    - "When the champion underperforms GARCH by the documented threshold, the monitor returns should_retrain=True"
    - "On degradation an alert is delivered to ALERT_WEBHOOK_URL when set, else appended to data/monitoring/alerts.jsonl — never crashing the pipeline"
    - "Before enough history accumulates (cold-start), the monitor returns should_retrain=False and never fires a false alert"
  artifacts:
    - path: "src/volforecast/monitoring/performance.py"
      provides: "Rolling-QLIKE champion-vs-GARCH performance monitor"
      contains: "def check_performance_drift"
      min_lines: 50
    - path: "src/volforecast/monitoring/alerts.py"
      provides: "Slack-compatible webhook POST with JSONL fallback (stdlib only)"
      contains: "def send_alert"
    - path: "tests/unit/test_performance.py"
      provides: "Cold-start, degradation-trigger, and no-trigger tests"
    - path: "tests/unit/test_alerts.py"
      provides: "Webhook-success, webhook-failure-fallback, and unset-env-fallback tests"
  key_links:
    - from: "src/volforecast/monitoring/performance.py"
      to: "volforecast.eval.metrics.qlike"
      via: "canonical QLIKE on champion and garch_baseline rows"
      pattern: "qlike"
    - from: "src/volforecast/monitoring/performance.py"
      to: "data/monitoring/forecast_vs_realized.parquet"
      via: "rolling window read of champion + garch_baseline rows"
      pattern: "forecast_vs_realized"
    - from: "src/volforecast/monitoring/alerts.py"
      to: "ALERT_WEBHOOK_URL env / data/monitoring/alerts.jsonl"
      via: "urllib POST with jsonl fallback"
      pattern: "ALERT_WEBHOOK_URL"
---

<objective>
Build the performance-degradation monitor (MON-03) and the alert channel (MON-04): compute rolling 21-day QLIKE for the champion and the GARCH baseline over `forecast_vs_realized.parquet`; when the champion underperforms GARCH by a documented relative threshold, return a retrain flag and fire an alert via a configurable webhook (with a JSONL fallback). This is the authoritative retrain trigger — stronger than distribution drift.

Purpose: The honest Phase-3 result (LightGBM loses to HAR-RV/GARCH on several assets) makes this monitor genuinely meaningful — champion underperformance vs GARCH is the real, expected signal. Independent of the drift adapter; depends on Plan 01 for the forecast_vs_realized contract (champion + garch_baseline rows).

Output: `src/volforecast/monitoring/performance.py`, `src/volforecast/monitoring/alerts.py`, and offline tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-orchestration-retraining/04-CONTEXT.md
@.planning/phases/04-monitoring-orchestration-retraining/04-RESEARCH.md
@.planning/phases/04-monitoring-orchestration-retraining/04-01-PLAN.md

<interfaces>
<!-- Executor: use these directly. forecast_vs_realized schema comes from Plan 04-01. -->

From src/volforecast/monitoring/labeller.py (Plan 04-01 — the forecast_vs_realized contract this monitor reads):
```python
FVR_SCHEMA = ["as_of_date","asset","horizon","model_version","model_alias","forecast_var","realized_var"]
LABEL_KEY = ["asset","as_of_date","model_alias"]
# Champion rows: model_alias="champion". GARCH baseline rows: model_alias="garch_baseline".
```

From src/volforecast/eval/metrics.py (canonical — shared by promotion gate too):
```python
def qlike(rv_var: np.ndarray, forecast_var: np.ndarray) -> float:
    # Patton variance form, qlike(x,x)==0, floored at 1e-10, raises on non-finite inputs.
    # Argument order: qlike(realized, forecast).
```

Performance monitor parameters (04-RESEARCH.md Pattern 3 — documented Claude's-discretion constants):
```python
PERF_WINDOW = 21          # rolling window in days
DEGRADATION_THRESHOLD = 0.10   # champion QLIKE > 1.10 * GARCH QLIKE triggers; document rationale
# Cold-start: require min(len(champion_rows), len(garch_rows)) >= PERF_WINDOW // 2 before firing.
```

Alert channel (04-RESEARCH.md Pattern 4 — stdlib only, no new deps):
```python
# ALERT_WEBHOOK_URL env var (Slack-compatible JSON POST). Unset/failure -> append to data/monitoring/alerts.jsonl.
# Must NEVER raise / crash the pipeline. timeout=5s on the POST.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alert delivery with webhook + JSONL fallback</name>
  <files>src/volforecast/monitoring/alerts.py, tests/unit/test_alerts.py</files>
  <read_first>
    - 04-RESEARCH.md Pattern 4 + "Verified: Webhook POST with urllib" code example
    - 04-RESEARCH.md Security Domain (ALERT_WEBHOOK_URL from env, never committed; payload contains no secrets)
    - 04-RESEARCH.md Assumption A2 (jsonl append concurrency — single-process Prefect task; threading.Lock optional)
  </read_first>
  <behavior>
    - Test 1 (webhook success): with ALERT_WEBHOOK_URL set and the POST monkeypatched to succeed, send_alert returns/records success and does NOT write to alerts.jsonl.
    - Test 2 (webhook failure → fallback): with ALERT_WEBHOOK_URL set but the POST raising URLError, send_alert swallows the error and appends one structured record to alerts.jsonl (never raises).
    - Test 3 (env unset → fallback): with ALERT_WEBHOOK_URL unset, send_alert appends one structured record to alerts.jsonl.
    - Test 4 (no secret leakage): the alert payload/body contains only title + provided metric fields (no env values, no tokens).
  </behavior>
  <action>
    Create `src/volforecast/monitoring/alerts.py` per 04-RESEARCH.md Pattern 4. Implement `send_alert(title: str, body: dict, *, webhook_url: str | None = None, jsonl_path: Path | None = None) -> None` (fire-and-forget): resolve webhook_url from the `webhook_url` arg else `os.environ.get("ALERT_WEBHOOK_URL","")`; resolve jsonl_path else `data/monitoring/alerts.jsonl`. Build a record `{"timestamp": utc-iso-Z, "title": title, **body}`. If a webhook URL is present, POST a Slack-compatible JSON payload (`{"text": ...}`) via `urllib.request` with `timeout=5`; on `(URLError, OSError)` fall through to the JSONL fallback (catch only those network errors — do not blanket-catch). The JSONL fallback mkdir-parents and appends one JSON line. The function must NEVER raise (document this contract). Read the webhook URL only from the env/arg — never hardcode it (Security Domain). Keep the parameters injectable so tests can point jsonl_path at tmp_path and pass a fake webhook_url.

    Create `tests/unit/test_alerts.py`: hermetic, offline. Monkeypatch `urllib.request.urlopen` (or the module-level POST helper) to simulate success and URLError. Use tmp_path for jsonl_path and `monkeypatch.delenv`/`setenv` for ALERT_WEBHOOK_URL. Implement the four behavior tests.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_alerts.py -q && uv run ruff check src/volforecast/monitoring/alerts.py tests/unit/test_alerts.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 alert tests pass.
    - `send_alert` never raises on webhook failure or unset env — it falls back to alerts.jsonl.
    - The webhook URL is read from `ALERT_WEBHOOK_URL` / the injected arg only; grep finds no hardcoded URL literal in alerts.py.
    - Uses stdlib `urllib`/`json` only — no new dependency added.
    - `uv run ruff check` clean.
  </acceptance_criteria>
  <done>Alerts deliver via webhook when configured, fall back to JSONL otherwise, and never crash the pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rolling-QLIKE champion-vs-GARCH performance monitor</name>
  <files>src/volforecast/monitoring/performance.py, tests/unit/test_performance.py</files>
  <read_first>
    - 04-RESEARCH.md Pattern 3 (rolling QLIKE) + Pitfall 3 (cold-start false alert)
    - src/volforecast/eval/metrics.py (qlike contract — argument order, flooring, non-finite raise)
    - src/volforecast/monitoring/labeller.py (FVR_SCHEMA, model_alias values "champion"/"garch_baseline")
  </read_first>
  <behavior>
    - Test 1 (cold-start): a forecast_vs_realized frame with fewer than PERF_WINDOW//2 champion or garch rows returns (False, {"reason":"cold_start"}) — no alert.
    - Test 2 (degradation triggers): champion forecasts deliberately worse than GARCH over the recent window (champion QLIKE > 1.10 * garch QLIKE) returns should_retrain=True with both QLIKE values in the report dict.
    - Test 3 (no trigger): champion better than or comparable to GARCH returns should_retrain=False.
    - Test 4 (canonical metric reuse): the monitor computes QLIKE via eval/metrics.qlike (not a re-implementation) — verified by a known-input assertion matching qlike() output.
  </behavior>
  <action>
    Create `src/volforecast/monitoring/performance.py` per 04-RESEARCH.md Pattern 3. Define documented constants `PERF_WINDOW = 21` and `DEGRADATION_THRESHOLD = 0.10` with a comment block stating the rationale (10% relative QLIKE underperformance over 21 obs is clearly meaningful; random noise rarely produces it persistently; this is Claude's discretion per CONTEXT.md — document the choice clearly). Implement `check_performance_drift(fvr: pd.DataFrame) -> tuple[bool, dict]`: take the most recent window of rows (sorted by as_of_date), split into `model_alias=="champion"` and `model_alias=="garch_baseline"`; enforce the cold-start gate `min(n_champion, n_garch) >= PERF_WINDOW // 2` returning `(False, {"reason":"cold_start", ...})` otherwise (Pitfall 3); compute `champ_qlike = qlike(champion.realized_var, champion.forecast_var)` and `garch_qlike = qlike(garch.realized_var, garch.forecast_var)` using the canonical metric; set `degraded = champ_qlike > garch_qlike * (1 + DEGRADATION_THRESHOLD)`; return `(degraded, {champion_qlike, garch_qlike, threshold, n_champion, n_garch, degraded})`. Add a convenience `run_performance_monitor(fvr_path: Path, *, alert_fn=send_alert) -> bool` that reads the parquet, calls check_performance_drift, and on degradation calls the injected alert function with a clear title and the report dict — returning the should_retrain bool for the Prefect flow (Plan 05) to consume. Import `send_alert` from `volforecast.monitoring.alerts`.

    Create `tests/unit/test_performance.py`: hermetic, offline. Build synthetic forecast_vs_realized frames (champion + garch rows) sized to exercise cold-start vs full-window. For Test 2, set champion forecast_var far from realized while GARCH forecast_var near realized so champion QLIKE clearly exceeds 1.10x GARCH. Pass a stub alert_fn (records calls) to assert run_performance_monitor fires exactly once on degradation and zero times otherwise.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_performance.py -q && uv run ruff check src/volforecast/monitoring/performance.py tests/unit/test_performance.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 performance tests pass.
    - Cold-start returns (False, {"reason":"cold_start"}) — no alert fired before PERF_WINDOW//2 rows for both aliases.
    - Degradation (champion QLIKE > 1.10 * GARCH QLIKE) returns should_retrain=True and fires exactly one alert via the injected alert_fn.
    - QLIKE is computed via `volforecast.eval.metrics.qlike` only (no parallel metric implementation).
    - DEGRADATION_THRESHOLD and PERF_WINDOW are named module constants with a documented rationale comment.
    - `uv run ruff check` clean.
  </acceptance_criteria>
  <done>The monitor compares rolling champion vs GARCH QLIKE, is cold-start-safe, returns a retrain flag, and fires an alert on degradation through the configurable channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forecast_vs_realized parquet → performance monitor | File-shaped input feeds the retrain decision |
| alert payload → external webhook | Data leaves the machine over HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | webhook payload / URL | mitigate | ALERT_WEBHOOK_URL read from env/arg only, never committed (Security Domain); payload carries title + QLIKE metrics only — no secrets, tokens, or env values (Task-1 Test 4) |
| T-04-08 | Denial of Service | alert delivery failure | mitigate | send_alert catches (URLError, OSError) with timeout=5 and falls back to alerts.jsonl; never raises, so a down webhook cannot crash the daily flow |
| T-04-09 | Tampering | QLIKE inputs | mitigate | qlike() floors inputs at 1e-10 and raises on non-finite values; the monitor uses the canonical metric so a malformed forecast cannot silently skew the retrain decision (V5) |
| T-04-10 | Elevation of Privilege | retrain flag vs promotion | mitigate | The monitor returns a retrain flag only; it never flips an MLflow alias. Promotion remains gated in Plan 04 (taxonomy: performance degradation → alert + retrain flag, NOT promotion) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_performance.py tests/unit/test_alerts.py -q` — all pass (offline, hermetic).
- `uv run ruff check .` — clean.
- Manual (execution-time, optional): with `data/monitoring/forecast_vs_realized.parquet` populated by Plan 01, `run_performance_monitor` returns a bool and (if degraded) writes an alerts.jsonl record when ALERT_WEBHOOK_URL is unset.
</verification>

<success_criteria>
- MON-03 satisfied: rolling live champion QLIKE vs GARCH rolling QLIKE triggers a retrain flag on documented degradation.
- MON-04 satisfied: alerts are delivered via the configurable webhook channel with a JSONL fallback, never crashing the pipeline.
- Cold-start safe; canonical QLIKE reused; promotion never triggered from this path.
</success_criteria>

<output>
Create `.planning/phases/04-monitoring-orchestration-retraining/04-03-SUMMARY.md` when done.
</output>
