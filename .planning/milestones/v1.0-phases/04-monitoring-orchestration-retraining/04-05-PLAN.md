---
phase: 04-monitoring-orchestration-retraining
plan: "05"
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - pipelines/daily_pipeline.py
  - scripts/create_deployment.py
  - infra/docker-compose.yml
  - tests/integration/test_daily_flow.py
autonomous: false
requirements: [ORCH-01, ORCH-02]
user_setup:
  - service: prefect
    why: "Register the daily deployment against the running compose Prefect server"
    env_vars:
      - name: PREFECT_API_URL
        source: "Set to http://localhost:4200/api for the host shell that runs scripts/create_deployment.py"
must_haves:
  truths:
    - "A single Prefect daily flow runs ingest → validate → features → label → drift-check → (conditional retrain → eval → register challenger) → promotion-gate end to end"
    - "Retraining runs when the performance-drift flag is set OR on the daily schedule force path — not only on schedule"
    - "Retraining registers the new version with @challenger (never auto-champion); the promotion gate is the only path to @champion"
    - "The flow is offline-testable (prefect_test_harness + task.fn) with external calls mocked"
    - "The compose Prefect worker can see the repo code so flow.from_source(local path) resolves on local-pool"
  artifacts:
    - path: "pipelines/daily_pipeline.py"
      provides: "Prefect @flow daily_flow wrapping existing scripts/functions as @tasks"
      contains: "@flow"
      min_lines: 90
    - path: "scripts/create_deployment.py"
      provides: "One-time deployment registration (local-pool, cron) against the compose server"
      contains: "work_pool_name=\"local-pool\""
    - path: "tests/integration/test_daily_flow.py"
      provides: "Offline flow/task tests via prefect_test_harness + .fn() with mocks"
  key_links:
    - from: "pipelines/daily_pipeline.py"
      to: "volforecast.monitoring.{labeller,drift,performance,promotion}"
      via: "thin @task wrappers over Plans 01-04 functions"
      pattern: "from volforecast.monitoring"
    - from: "scripts/create_deployment.py"
      to: "prefect-server (compose) via PREFECT_API_URL"
      via: "flow.from_source(repo_root).deploy(work_pool_name='local-pool', cron=...)"
      pattern: "from_source"
    - from: "infra/docker-compose.yml"
      to: "prefect-worker code visibility"
      via: "bind-mount the repo so local-process flow runs find pipelines/"
      pattern: "volumes"
---

<objective>
Wire the closed loop into one Prefect 3 daily flow (ORCH-01, ORCH-02): `ingest → validate → features → label → drift-check → (conditional retrain → eval → register challenger) → promotion-gate`, as thin `@task` wrappers over existing scripts and the Plan 01-04 monitoring functions. Retraining is triggered by the performance-drift flag in addition to the daily schedule. Register the deployment against the compose Prefect server on `local-pool` with a daily cron, and add the worker code-visibility mount so `flow.from_source` resolves.

Purpose: Final vertical slice — after this plan the whole system runs on a schedule and reacts to performance degradation, with promotion still gated. This plan composes the prior four plans; it does NOT reimplement their logic.

Output: `pipelines/daily_pipeline.py`, `scripts/create_deployment.py`, an `infra/docker-compose.yml` worker mount, and `tests/integration/test_daily_flow.py`. Deployment registration + one manual flow run are execution-time (live) verifications behind a checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-orchestration-retraining/04-CONTEXT.md
@.planning/phases/04-monitoring-orchestration-retraining/04-RESEARCH.md
@.planning/phases/04-monitoring-orchestration-retraining/04-01-PLAN.md
@.planning/phases/04-monitoring-orchestration-retraining/04-03-PLAN.md
@.planning/phases/04-monitoring-orchestration-retraining/04-04-PLAN.md

<interfaces>
<!-- Executor: wrap these as @tasks. Call existing scripts/functions as black boxes. -->

Existing scripts to wrap as black-box subprocess/function calls (DO NOT modify them — a concurrent fixer owns train_lgbm.py / eval_lgbm.py):
```
volforecast ingest                          # CLI (volforecast.cli:main) — cache-first validated ingest of all assets
python scripts/generate_features.py         # writes data/features/{class}/{slug}.parquet for 5 assets
python scripts/run_labeller.py              # Plan 04-01 — appends to data/monitoring/forecast_vs_realized.parquet
python scripts/train_lgbm.py                # Phase 3 — MLflow run + register volforecast-lgbm (sets @champion on first train)
python scripts/eval_lgbm.py                 # Phase 3 — ml_vs_baselines report
```

Champion model loading (the unified pattern the fixer standardizes on — use this, do not access pyfunc internals here):
```python
import mlflow.lightgbm
model = mlflow.lightgbm.load_model("models:/volforecast-lgbm@champion")
```

Plan 01-04 monitoring functions to wrap as tasks:
```python
from volforecast.monitoring.labeller import label_forecasts        # (data_root, fvr_path) -> int net-new rows
from volforecast.monitoring.drift import run_distribution_drift, select_numerical_columns
from volforecast.monitoring.performance import run_performance_monitor   # (fvr_path, alert_fn=...) -> bool should_retrain
from volforecast.monitoring.promotion import frozen_window_qlike, promote_if_better  # ORCH-03 gate
```

Prefect 3 deployment (04-RESEARCH.md Pattern 5 + "Verified: Prefect deployment script"):
```python
daily_flow.from_source(
    source=str(repo_root),                                  # local path on the worker-mounted volume
    entrypoint="pipelines/daily_pipeline.py:daily_flow",
).deploy(name="volforecast-daily", work_pool_name="local-pool", cron="0 8 * * *")  # 08:00 UTC
# Run from HOST shell: PREFECT_API_URL=http://localhost:4200/api python scripts/create_deployment.py  (Open Question #3 resolution)
```

Prefect offline testing (04-RESEARCH.md Pattern 7):
```python
from prefect.testing.utilities import prefect_test_harness    # session fixture: in-memory server
# task.fn() bypasses Prefect state for direct business-logic tests; mock subprocess/MLflow calls.
```

Compose facts (infra/docker-compose.yml):
- prefect-worker runs `prefect worker start --pool local-pool --type process` with PREFECT_API_URL=http://prefect-server:4200/api.
- prefect-worker currently has NO repo/code volume mount (only the api service mounts ../data:/data). This must be fixed for local-process flow runs (Pitfall 4 / Assumption A4).
- Host PREFECT_API_URL for the deployment script = http://localhost:4200/api.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Prefect daily flow as thin task wrappers</name>
  <files>pipelines/daily_pipeline.py</files>
  <read_first>
    - 04-RESEARCH.md Pattern 5 (flow/task structure) + System Architecture Diagram (task order) + Pitfall 5 (retrain flag must be ephemeral — prefer flow parameter, no stale flag file)
    - pipelines/__init__.py (existing package marker)
    - scripts/run_labeller.py (Plan 04-01 entry), scripts/generate_features.py (feature regen), src/volforecast/cli.py (ingest entry)
    - src/volforecast/monitoring/performance.py + promotion.py (Plans 03/04 function signatures)
  </read_first>
  <action>
    Create `pipelines/daily_pipeline.py` with a single `@flow(name="volforecast-daily")` named `daily_flow(force_retrain: bool = False) -> dict`. Implement the tasks in the locked order (CONTEXT.md): `ingest → validate → features → label → drift-check → (conditional retrain → eval → register-challenger) → promotion-gate`. Each step is a thin `@task` that calls an existing script/function as a black box — NO reimplementation:
    - `ingest_validate_task` (@task(retries=2, retry_delay_seconds=30)): invoke the `volforecast ingest` CLI (subprocess, project root cwd) — validation is built into the ingest gate, so this single task covers ingest+validate. On non-zero exit, raise so Prefect surfaces a data-quality hard-fail (locked taxonomy: data-quality failure → pipeline hard-fail).
    - `features_task`: run `scripts/generate_features.py` (subprocess).
    - `label_task`: call `label_forecasts(data_root, fvr_path)`; return net-new row count.
    - `drift_check_task`: read the frozen reference + a current feature/prediction frame, call `run_distribution_drift(...)`; write dated reports. This task is report-only (never returns a promotion signal).
    - `performance_check_task` -> bool: call `run_performance_monitor(fvr_path)`; return the should_retrain flag (this is the retrain trigger — MON-03).
    - `retrain_task`: run `scripts/train_lgbm.py` BUT ensure the new version is registered as `@challenger`, not `@champion` — document that retraining in the flow context must set the challenger alias (if train_lgbm.py sets @champion on its own, the flow must immediately reassign the freshly-registered version to @challenger via MlflowClient so auto-champion never happens in the loop; never auto-promote). Then run `scripts/eval_lgbm.py`.
    - `promotion_gate_task`: compute `frozen_window_qlike` for champion vs challenger over a frozen window from forecast_vs_realized and call `promote_if_better(...)` (ORCH-03). Default no-promote.

    In `daily_flow`: chain ingest→features→label→drift; compute `should_retrain = performance_check_task() or force_retrain` (the flow parameter is the ephemeral trigger — Pitfall 5, no stale flag file). If `should_retrain`: run retrain + eval + register-challenger, then promotion_gate. If not retraining, skip the gate (nothing new to promote). Return a summary dict (rows_labelled, drift_report_path, should_retrain, promoted) for observability. Use Prefect's logger. Keep all paths resolved from `volforecast.config.project_root()` so the flow works both on the host and in the worker container.
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('pipelines/daily_pipeline.py').read()); print('parses')" && uv run python -c "from pipelines.daily_pipeline import daily_flow; print(getattr(daily_flow,'name',None))"</automated>
  </verify>
  <acceptance_criteria>
    - `daily_flow` imports and exposes the Prefect flow name "volforecast-daily".
    - Task order matches the locked sequence; each task wraps an existing script/function (no reimplemented labeller/drift/performance/promotion logic).
    - Retrain registers a `@challenger` (never auto-`@champion`); promotion happens only through `promote_if_better`.
    - The retrain trigger is the ephemeral flow parameter / performance flag (no persistent flag file left behind — Pitfall 5).
    - Drift check is report-only and returns no promotion signal.
  </acceptance_criteria>
  <done>The daily flow composes Plans 01-04 in the locked order with conditional flag-or-schedule retraining and a gated promotion step.</done>
</task>

<task type="auto">
  <name>Task 2: Offline flow tests + deployment script + worker code-visibility mount</name>
  <files>tests/integration/test_daily_flow.py, scripts/create_deployment.py, infra/docker-compose.yml</files>
  <read_first>
    - 04-RESEARCH.md Pattern 7 + "Verified: Prefect 3 test harness" + Pattern 5 deployment example
    - 04-RESEARCH.md Pitfall 4 (work_pool_name must equal local-pool) and Open Question #3 (deployment runs from HOST with PREFECT_API_URL=http://localhost:4200/api)
    - infra/docker-compose.yml lines 98-138 (prefect-worker has no code mount; api mounts ../data:/data) — add the worker mount here
    - tests/integration/test_api.py (synthetic fixture + offline pattern to mirror)
  </read_first>
  <action>
    Create `tests/integration/test_daily_flow.py`: offline, hermetic. Add a session-scoped autouse fixture using `prefect_test_harness()` (in-memory server). Test the flow logic without live services by monkeypatching the subprocess/script calls and the MLflow load, and pointing data paths at a tmp synthetic tree (reuse the synthetic-processed/predictions fixture pattern from test_api.py). Cover: (1) happy path with should_retrain=False skips retrain+promotion; (2) force_retrain=True (or a stubbed performance flag) runs retrain→eval→promotion-gate exactly once; (3) the promotion gate defaults to no-promote when the challenger does not beat the champion (mocked QLIKE). Use `task.fn(...)` for direct business-logic assertions where a full flow run is unnecessary.

    Create `scripts/create_deployment.py` per 04-RESEARCH.md Pattern 5 (Open Question #3 resolution — runs from the HOST shell): import `daily_flow`, read `PREFECT_API_URL` from env defaulting to `http://localhost:4200/api`, call `daily_flow.from_source(source=str(repo_root), entrypoint="pipelines/daily_pipeline.py:daily_flow").deploy(name="volforecast-daily", work_pool_name="local-pool", cron="0 8 * * *")`. `work_pool_name` MUST be exactly `local-pool` to match the compose worker (Pitfall 4). Add a module docstring with the exact host run command: `PREFECT_API_URL=http://localhost:4200/api python scripts/create_deployment.py`, and the cron rationale (08:00 UTC — after Binance daily close, before equity opens).

    Edit `infra/docker-compose.yml` prefect-worker service to add a code-visibility volume so a local-process flow run can resolve `flow.from_source(source=<repo_root>)` (Assumption A4 / Pitfall 4): bind-mount the repo root into the worker at the same absolute path the deployment's `source` records, OR mount the repo and document that `create_deployment.py` must be run with a source path that exists inside the worker container. Add the data mount too if the flow's ingest/features steps run inside the worker (`../data:/data` + the VOLFORECAST_DATA_ROOT/VOLFORECAST_ROOT envs mirroring the api service). Keep loopback-only port posture unchanged. Add a comment explaining why the worker now needs the code mount (process work pool runs the entrypoint file from the local filesystem).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_daily_flow.py -q && uv run python -c "import ast; ast.parse(open('scripts/create_deployment.py').read()); assert 'local-pool' in open('scripts/create_deployment.py').read(); print('ok')" && uv run python -c "import yaml; d=yaml.safe_load(open('infra/docker-compose.yml')); w=d['services']['prefect-worker']; assert 'volumes' in w, 'worker needs a code/data volume mount'; print('worker mount present')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_daily_flow.py -q` passes offline (prefect_test_harness; no live server/MLflow/network).
    - `scripts/create_deployment.py` uses `work_pool_name="local-pool"`, reads PREFECT_API_URL from env (default http://localhost:4200/api), and documents the host run command.
    - `infra/docker-compose.yml` prefect-worker now declares a `volumes:` entry giving the worker access to the repo code (and data) so `flow.from_source(local path)` resolves on local-pool.
    - The deployment cron is `0 8 * * *` with documented rationale.
  </acceptance_criteria>
  <done>The flow is offline-tested, the deployment script targets local-pool with a daily cron, and the compose worker can see the repo code for local-process runs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Registered the `volforecast-daily` Prefect deployment against the running compose server (local-pool, cron 08:00 UTC) and verified an end-to-end manual flow run. Compose stack is UP (prefect-server 127.0.0.1:4200, prefect-worker on local-pool, mlflow 127.0.0.1:5000, api 127.0.0.1:8000).</what-built>
  <how-to-verify>
    1. From the HOST shell with the compose stack running: `PREFECT_API_URL=http://localhost:4200/api uv run python scripts/create_deployment.py`. Confirm it prints a deployment id / name `volforecast-daily` with no error.
    2. Open the Prefect UI at http://localhost:4200 → Deployments → confirm `volforecast-daily` exists, is on work pool `local-pool`, and shows the cron schedule `0 8 * * *`.
    3. Trigger a manual run: from the UI "Run" → "Quick run" (or `PREFECT_API_URL=http://localhost:4200/api uv run prefect deployment run 'volforecast-daily/volforecast-daily'`). Watch the run in the UI.
    4. Confirm the run transitions Scheduled → Running → Completed (NOT stuck "Scheduled" — that indicates the work-pool-name mismatch in Pitfall 4, or the worker cannot see the code, indicating the volume mount needs adjustment).
    5. Confirm `data/monitoring/forecast_vs_realized.parquet` gained rows and a dated `data/monitoring/{date}_drift.html` was written.
    6. Confirm no challenger was auto-promoted (default no-promote): the `@champion` alias in MLflow is unchanged unless the gate genuinely fired.
  </how-to-verify>
  <resume-signal>Type "approved" once the deployment registers and a manual run completes end-to-end with monitoring outputs produced, or describe where the run stalled (e.g. stuck Scheduled → worker pool/mount issue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prefect server/worker (compose) → flow execution | The flow runs scripts that touch data, MLflow, and the webhook |
| deployment registration → Prefect API | A new scheduled deployment enters the orchestration server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Elevation of Privilege | flow retrain path | mitigate | retrain_task registers `@challenger` only; the alias flip to @champion is reachable solely via promote_if_better (Plan 04). Drift check returns no promotion signal — taxonomy guard enforced in flow wiring |
| T-04-16 | Denial of Service | stuck/stale retrain trigger | mitigate | Retrain trigger is the ephemeral flow parameter / per-run performance flag — no persistent flag file to leave set (Pitfall 5); cooldown in the gate prevents retrain-storm promotions |
| T-04-17 | Information Disclosure | Prefect/MLflow UIs on LAN | accept | Existing compose binds 127.0.0.1 only (WR-05); this plan does not add LAN-exposed ports |
| T-04-18 | Tampering | worker code mount | mitigate | The added worker bind-mount is read of the project's own repo on the local machine; loopback-only posture unchanged; no new published port |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (evidently added in Plan 02 behind its own legitimacy checkpoint); Prefect/MLflow already pinned and present |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_daily_flow.py -q` — passes offline (prefect_test_harness).
- `uv run ruff check .` — clean.
- Live (checkpoint): deployment registers on local-pool with cron; a manual run completes Scheduled→Running→Completed and produces monitoring outputs; no auto-promotion.
</verification>

<success_criteria>
- ORCH-01 satisfied: the daily Prefect DAG runs ingest → validate → features → (train → eval → register on retrain) → promotion-gate end-to-end on schedule and on manual trigger.
- ORCH-02 satisfied: retraining is triggered by the performance-drift flag in addition to the schedule (force_retrain / performance flag path).
- Phase-4 loop closes: monitoring outputs are produced on a schedule, retraining is conditional, and promotion stays gated (default no-promote).
</success_criteria>

<output>
Create `.planning/phases/04-monitoring-orchestration-retraining/04-05-SUMMARY.md` when done.
</output>
